# Efficiency journal: auth-core lane (phase 2 stack A)

One entry per task. Fill in as you go, not afterwards. Times are local (Europe/Belgrade).
Approach values: `inline` (Claude in main session), `subagent` (dispatched agent), `skill`
(named skill), `parallel-session` (separate worktree + session), `human`.

| Date | Task | Approach | Start | End | Rework | Critic findings (total / accepted / prevented rework) | Notes |
|---|---|---|---|---|---|---|---|
| 2026-09-24 | Task 02: `security-reviewer` agent (plugin) | subagent (sonnet) | 14:35 | 14:46 | none | n/a (dry run, not a critic pass) | TDD RED confirmed first (`agents.test.mjs` failed: `ships the security reviewer next to the plan critic` — only `plan-critic.md` present — and `ENOENT` on `security-reviewer.md`), then GREEN after writing the agent (90/90 vitest across 8 files: the 7 existing phase 0 files — `format`, `hooks-cli`, `hooks-json`, `project-root`, `protect`, `reminder`, `settings` — plus 6 new `agents.test.mjs` tests, matching the brief's 1+2+2+1 split). Both `claude plugin validate --strict` runs (marketplace, plugin) printed "Validation passed". Dry run on a seeded vulnerability (`scratch/vuln.ts`: token logged to console, no route-access marker, no zod schema) on a throwaway `tmp/security-reviewer-dry-run` branch, deleted after: the agent's table found all 3 seeded issues (exceeding the brief's ≥2/3 bar) plus 3 more (unvalidated/unconsumed token, dead code outside module structure, missing negative tests), verdict `BLOCK`; full table: (1) CRITICAL token logged in cleartext `scratch/vuln.ts:5`; (2) HIGH token never validated/consumed; (3) HIGH no `@RequirePermissions`/`@Public` marker; (4) HIGH no zod schema; (5) MEDIUM dead code outside module structure; (6) MEDIUM no negative tests for token handling. Sequencing note: the dry-run script requires a clean tree before creating its temp branch (it needs the new agent file present to review anything meaningful), so the source commit (agent, test, README) was made first and this journal — which needed the dry run's real output — was committed as a second commit on the same branch/PR, rather than bundled into one commit as the brief's Step 6 literally shows. |
| 2026-09-24 | Task 03: auth-core — package, tokens, secret cipher | subagent (sonnet) | 15:02 | 15:50 | two fix-round commits after two `security-reviewer` passes (round 1 verdict `FIX BEFORE MERGE`: 1 high, 5 medium, 3 low; round 2 verdict `FIX BEFORE MERGE`: 0 high, 1 medium, 5 low) | n/a (no critic pass scheduled for this task; the `security-reviewer` tables are recorded in the Notes column instead) | TDD RED confirmed first (`pnpm turbo run build --filter=@tms/auth-core` failed with `TS18003: No inputs were found` before `src/` existed; then `pnpm turbo run test` failed all 3 suites — `purity.spec.ts` on the missing `src` directory, `tokens.spec.ts`/`secret-cipher.spec.ts` on `Cannot find module '../src'`), then GREEN after implementing `random.ts`, `tokens.ts`, `secret-cipher.ts`, `index.ts` verbatim from the plan (24/24 Jest tests). `lint`, `typecheck`, `build` each green; `dist/index.js` and `dist/index.d.ts` present. Lint probe (`@nestjs/common`, `@prisma/client`, `@tms/contracts` in a throwaway `src/lint-probe.ts`) gave exit=1 with exactly 3 `no-restricted-imports` errors and no extra boundaries error; removing the probe file gave exit=0. No deviations from the plan's literal file contents: the `packages.auth-core` boundaries entry and policy (`packages/config/eslint/base.mjs`) were already wired by the phase 1 stack, so the first commit only added the package itself. The `security-reviewer` agent was not registered as a directly invocable subagent in this session despite being merged in Task 02 (`Agent type 'ethernal-nest-react:security-reviewer' not found`), so both rounds were run manually by handing its full checklist and persona (`tools/claude-plugin/ethernal-nest-react/agents/security-reviewer.md`) to a general-purpose agent instructed to follow it verbatim — a fallback worth fixing (plugin/session reload) before the next auth PR. Round 1 findings: (1) HIGH — GCM AAD did not bind the envelope's own version/key id, so an envelope could in principle be relabelled to a different key id without failing authentication if that id's bytes matched (`secret-cipher.ts`); (2) MEDIUM — an injected `RandomSource` returning too few bytes silently produced a short IV or token with no length check; (3) MEDIUM — key material was visible via `util.inspect`/`JSON.stringify` of the cipher instance (TS `private` is not a runtime private field); (4) MEDIUM — an invalid `activeKeyId` or a hostile envelope key id could be echoed into the thrown error message; (5) MEDIUM — an empty AAD was accepted, binding a secret to no owner; (6) MEDIUM — no negative tests for a truncated tag/IV, wrong version or wrong part count (Node accepts short GCM tags unless `authTagLength` is pinned); (7) LOW — `safeEqual` branched on length before comparing (a timing signal) and, hashed as UTF-8, two different lone surrogates could compare equal; (8) LOW — `parseKeyring`'s plain `{}` accumulator let a `__proto__` key id write the prototype instead of an own property; (9) LOW — base64 decoding was lenient (deferred). Fixed 1, 3, 4, 5, 6, 7, 8 and the length-check half of 2 (13 new negative/property tests, 37/37 green); kept the injectable `RandomSource` seam itself (2's other half) because it is the mandated port per spec section 3, and deferred 9 as a follow-up — the auth tag already fails closed on any corrupted ciphertext, and tightening decoding risks rejecting valid keys from tools with different base64 dialects. Pushed and re-ran the reviewer (mandatory after a HIGH): it independently confirmed 1 and all five round-1 mediums resolved, confirmed 9 still open as expected, and surfaced 6 new items from reading the fix itself: (1) MEDIUM — the round-1 redaction test asserted the wrong things and would have passed against the unfixed code (a test-quality gap, not a product gap); (2) LOW — reiterated 9 with a request for a code comment/tracked follow-up; (3) LOW — a non-active key id in `keys` and `envelopeKeyId`'s return value were still echoed/returned unchecked; (4) LOW — `aad`/`plaintext` had the same UTF-8 lone-surrogate lossiness just fixed in `safeEqual`; (5) LOW — key bytes lived in a plain `Buffer` (inspectable, mutable by the caller after construction) rather than a `KeyObject`; (6) LOW — `cryptoRandomSource` was a mutable exported object. No CRITICAL/HIGH this round, so per the task's own instructions this did not mandate a third reviewer pass; fixed all 6 anyway in a second fix-round commit (rewrote the redaction test to assert the actual rendered/serialised shape, validated every key id's format before any echo, added a lone-surrogate check on `aad`/`plaintext`, switched to `crypto.createSecretKey` `KeyObject`s, and froze `cryptoRandomSource`) since the fixes were cheap and this is the one moment before any consumer exists; only 9 remains open, now with an inline code comment at both decode sites recording the deferral and its reasoning. Final: 42/42 Jest tests, `pnpm verify` green (41/41 turbo tasks, prettier, hygiene, gitleaks), CI (`verify`, `hygiene`, `db-drift`, `e2e`) green on both the initial push and the round-1 fix push. |
| 2026-09-24 | Task 04: auth-core — password hashing and policy | subagent (sonnet) | 15:20 | 16:35 | one fix-round commit after a `security-reviewer` pass (verdict `FIX BEFORE MERGE`: 0 critical, 0 high, 2 medium, 2 low) | n/a (no critic pass scheduled for this task; the `security-reviewer` table is recorded in the Notes column instead) | TDD RED confirmed first (`pnpm turbo run test --filter=@tms/auth-core` failed 10/52 — `TypeError: ... is not a function` on both `Argon2idPasswordHasher`/`DEFAULT_ARGON2_PARAMS` and `checkPassword`, neither exported yet), then GREEN after implementing `password-hasher.ts` and `password-policy.ts` verbatim from the plan and adding both exports to `index.ts` (60/60 Jest tests across the package: hasher 6, policy 10, plus the 44 pre-existing tokens/secret-cipher/purity tests unchanged). `pnpm install` added the four new dependencies (`@node-rs/argon2` 2.2.1, `@zxcvbn-ts/core` 4.2.0, `@zxcvbn-ts/language-common` 4.1.3, `@zxcvbn-ts/language-en` 4.1.1) with no `ERR_PNPM_IGNORED_BUILDS` line (no install script on `@node-rs/argon2`, as the plan notes). No deviations from the plan's literal file contents in the first commit. `pnpm verify` green (45/45 turbo tasks including the phase-1 `@tms/db`, `@tms/nest-bootstrap`, `@tms/logger`, `@tms/api-admin`, `@tms/api-driver` suites already on `main`; prettier check clean; hygiene ok; gitleaks no leaks). The `security-reviewer` agent was again not registered as a directly invocable subagent in this fresh worktree/session (`Agent type 'ethernal-nest-react:security-reviewer' not found`, same as Task 03), so it was run by handing its full checklist and persona (`tools/claude-plugin/ethernal-nest-react/agents/security-reviewer.md`) verbatim to a general-purpose (sonnet) agent — still unresolved after Task 03 flagged it as a follow-up. Findings, most severe first: (1) MEDIUM — the pepper was held as a plain `Buffer` field, visible via `util.inspect`/`JSON.stringify` of the hasher instance (the same class of issue fixed one PR earlier in `secret-cipher.ts` for the AES-GCM keys); (2) MEDIUM — `DEFAULT_ARGON2_PARAMS` was an unfrozen, aliased object, so a later mutation of it (or of a caller-supplied `params` object, stored by reference) would silently change an already-constructed hasher's cost parameters; (3) LOW — `needsRehash` compares `algorithm`/`memoryCost`/`timeCost`/`parallelism` but not `parseOptions`'s `version` field; (4) LOW — `checkPassword` bounds only `password`'s length before scoring, not `userInputs`'s size, so an attacker-sized `userInputs` list could defeat the scoring-cost bound. Fixed 1, 2 and 4 in one fix-round commit: the pepper is now a `crypto.createSecretKey` `KeyObject` (exported to bytes only transiently at each `hash`/`verify` call, mirroring `AesGcmSecretCipher`), `DEFAULT_ARGON2_PARAMS` is `Object.freeze`d and the constructor copies whichever params object it receives, and `checkPassword` now caps `userInputs` to 50 raw entries of at most 254 bytes each (RFC 5321's email length) before the split/filter that feeds `zxcvbn.check`; added 3 regression tests (pepper redaction, immunity to a mutated shared/caller params object, scoring-cost bound under a 100 000-element `userInputs` list) — 63/63 Jest tests green. Deferred 3 (LOW): every hash this class itself produces uses the argon2 library's current default version (`Options.version` is never set), so the gap only under-flags a hash crafted outside this class or by an older library version, and the four already-compared parameters still force a rehash on any real policy change; fixing it would also widen the `Argon2Params` shape the plan and the phase index pin for later tasks, which is not warranted for a LOW finding with no live exploitation path. No CRITICAL/HIGH, so per the task's own instructions a second reviewer pass was not mandated; not re-run. Final: 63/63 Jest tests, `pnpm verify` green (45/45 turbo tasks, prettier, hygiene, gitleaks). |
| 2026-09-24 | Task 05: auth-core — TOTP and recovery codes | subagent (sonnet) | 15:58 | 16:35 | two fix-round commits after two `security-reviewer` passes (round 1 verdict `FIX BEFORE MERGE`: 0 critical, 1 high, 2 medium, 2 low; round 2 verdict `OK`: 0 critical, 0 high, 0 medium, 2 low) | n/a (no critic pass scheduled for this task; the `security-reviewer` tables are recorded in the Notes column instead) | TDD RED confirmed first (`pnpm turbo run test --filter=@tms/auth-core` failed 2/7 suites, 9/72 tests — `TypeError: src_1.OtplibTotpProvider is not a constructor` on `totp.spec.ts` and `... is not a function` on `normalizeRecoveryCode`/`hashRecoveryCode` in `recovery-codes.spec.ts`, `otplib` not yet a dependency), then GREEN after implementing `totp.ts` and `recovery-codes.ts` verbatim from the plan and adding both exports to `index.ts` (91/91 Jest tests across the package: the 5 RFC 6238 SHA1 vectors, secret/URI shape, 3 accepted + 2 rejected step offsets, replay-of-`lastUsedStep`/older-step rejection with next-step acceptance, 5 malformed codes short-circuited before the library call, plus the recovery-code generation/entropy/normalisation/hashing cases, on top of the 63 pre-existing tokens/secret-cipher/password/purity tests unchanged). Catalog gained `otplib: 13.5.0` (`pnpm-workspace.yaml`) and the package dependency (`catalog:`); `pnpm install` resolved the six `@otplib/*` subpackages with no postinstall scripts. No deviations from the plan's literal file contents in the first commit. `pnpm verify` green (45/45 turbo tasks, prettier check clean, hygiene ok, gitleaks no leaks — the RFC 6238 test-vector secret's `gitleaks:allow` comment was carried over from the plan verbatim). PR #39 opened; the `security-reviewer` agent was again not registered as a directly invocable subagent in this fresh worktree/session, so both rounds were run by handing its full checklist and persona (`tools/claude-plugin/ethernal-nest-react/agents/security-reviewer.md`) verbatim to a general-purpose (sonnet) agent — same unresolved follow-up as Tasks 03/04. Round 1 findings, most severe first: (1) HIGH — `OtplibTotpProvider.verify()` could reject instead of resolving `{ ok: false }` when a stored `lastUsedStep` is ahead of the window computed from `now` (clock skew or a backward clock jump across replicas): otplib's compiled `@otplib/totp` throws `AfterTimeStepRangeExceededError` when `afterTimeStep` exceeds the max window counter, which the plan's own `verify()` signature (`Promise<TotpVerification>`, never documented as rejecting) did not guard against; reproduced directly against the library with `lastUsedStep: step + 100`; (2) MEDIUM — `generateRecoveryCodes()`/`encode()` didn't validate that the injected `RandomSource.bytes(10)` actually returned 10 bytes, unlike the guard `tokens.ts` already has for the same class of bug (from the Task 03 review); (3) MEDIUM — `normalizeRecoveryCode(input)` ran its case-fold/replace passes over unbounded `input` before any length check, unlike `code` in `totp.ts` which is gated by `/^\d{6}$/` first — a cost-shaped gap once an unauthenticated recovery-code route exists (Task 14/17); (4) LOW — `secret` isn't bound/format-validated before reaching otplib (deferred: it is only ever this class's own generated value or a caller's already-decrypted value in this package, never unvalidated external input); (5) LOW — no test covered an adversarial `lastUsedStep` beyond the window. Fixed 1, 2, 3 and 5 in one fix-round commit (`f539b6f`): `verify()` wraps the otplib call in try/catch and fails closed to `{ ok: false }` on any error, with a regression test asserting `lastUsedStep: step + 100` resolves rather than rejects; `generateRecoveryCodes()` now asserts `bytes.length === CODE_BYTES` and throws `RangeError` otherwise, with a test for both a too-short and too-long `RandomSource` response; `normalizeRecoveryCode()` now rejects any input over 64 characters before the transform chain, with a test; deferred 4 with an inline code comment on the `TotpProvider` interface explaining why (94/94 Jest tests, `pnpm verify` green). Pushed and re-ran the reviewer (mandatory after a HIGH): round 2 independently traced otplib's compiled source to confirm the exact throw condition and that the new regression test genuinely exercises it (would fail without the fix), confirmed findings 1, 2, 3 and 5 resolved and 4's deferral comment present, and surfaced one new LOW — the finding-3 regression test asserted only the return value (`null`), which a 65-character same-letter string would also produce from the *unfixed* code via the final format regex, so it didn't actually prove the length gate runs before the case-fold/replace passes — plus one informational LOW (the blanket `catch` in `verify()` turns any otplib error, not just clock skew, into an indistinguishable `{ok:false}` with no logging, worth a debug log at the future login/MFA call site in Task 11+, not a change to this package). Verdict `OK` (0/0/0/2). Fixed the new LOW: rewrote the test to spy on `String.prototype.toUpperCase` and assert it is never called for a 1,000,000-character input (but is called once for a valid code) — verified this version fails against the pre-fix source by temporarily reverting the length-gate line and re-running just that test (`Expected number of calls: 0, Received number of calls: 1`), then restored the fix. Deferred the informational LOW: it names a future caller this package doesn't have yet (Task 11+ login/MFA handler), not a gap in this PR's own code. Both review tables posted verbatim as PR comments on #39. Final: 94/94 Jest tests, `pnpm verify` green (45/45 turbo tasks, prettier, hygiene, gitleaks), CI (`verify`, `hygiene`, `db-drift`, `e2e`) green on both the initial push and the fix-round push. |
| 2026-09-24 | Task 06: auth-core — lockout, session expiry, step-up and MFA-attempt policies | subagent (sonnet) | 16:12 | TBD | TBD | n/a (no critic pass scheduled for this task; the `security-reviewer` table is recorded in the Notes column instead) | TDD RED confirmed first (`pnpm turbo run test --filter=@tms/auth-core` failed 3/10 suites, 17/111 tests — `TypeError` on every one of `DEFAULT_LOCKOUT_CONFIG`/`registerFailure`/`registerSuccess`/`isLocked`/`normalizeLockout`/`lockDurationSeconds`/`lockRemainingSeconds`, `absoluteExpiry`/`isSessionExpired`/`shouldTouch`, `isStepUpFresh`/`MFA_MAX_ATTEMPTS`/`registerMfaAttempt`, none exported yet), then GREEN after implementing `policies/lockout.ts`, `policies/session-expiry.ts`, `policies/step-up.ts`, `policies/mfa-attempts.ts` verbatim from the plan and re-exporting all four from `index.ts` (115/115 Jest tests across the package: lockout 6 incl. two fast-check properties at 100 runs each, session-expiry 6, step-up 6, plus the 94 pre-existing tokens/secret-cipher/password/totp/recovery-codes/purity tests unchanged). No deviations from the plan's literal file contents. `pnpm verify` green (45/45 turbo tasks, prettier check clean, hygiene ok, gitleaks no leaks). This is the last task of Stack A; `@tms/auth-core` is now feature-complete for Stack B. Notes column to be completed with the security-reviewer verdict, PR number and CI result once the PR is open. |
