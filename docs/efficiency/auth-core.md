# Efficiency journal: auth-core lane (phase 2 stack A)

One entry per task. Fill in as you go, not afterwards. Times are local (Europe/Belgrade).
Approach values: `inline` (Claude in main session), `subagent` (dispatched agent), `skill`
(named skill), `parallel-session` (separate worktree + session), `human`.

| Date | Task | Approach | Start | End | Rework | Critic findings (total / accepted / prevented rework) | Notes |
|---|---|---|---|---|---|---|---|
| 2026-09-24 | Task 02: `security-reviewer` agent (plugin) | subagent (sonnet) | 14:35 | 14:46 | none | n/a (dry run, not a critic pass) | TDD RED confirmed first (`agents.test.mjs` failed: `ships the security reviewer next to the plan critic` — only `plan-critic.md` present — and `ENOENT` on `security-reviewer.md`), then GREEN after writing the agent (90/90 vitest across 8 files: the 7 existing phase 0 files — `format`, `hooks-cli`, `hooks-json`, `project-root`, `protect`, `reminder`, `settings` — plus 6 new `agents.test.mjs` tests, matching the brief's 1+2+2+1 split). Both `claude plugin validate --strict` runs (marketplace, plugin) printed "Validation passed". Dry run on a seeded vulnerability (`scratch/vuln.ts`: token logged to console, no route-access marker, no zod schema) on a throwaway `tmp/security-reviewer-dry-run` branch, deleted after: the agent's table found all 3 seeded issues (exceeding the brief's ≥2/3 bar) plus 3 more (unvalidated/unconsumed token, dead code outside module structure, missing negative tests), verdict `BLOCK`; full table: (1) CRITICAL token logged in cleartext `scratch/vuln.ts:5`; (2) HIGH token never validated/consumed; (3) HIGH no `@RequirePermissions`/`@Public` marker; (4) HIGH no zod schema; (5) MEDIUM dead code outside module structure; (6) MEDIUM no negative tests for token handling. Sequencing note: the dry-run script requires a clean tree before creating its temp branch (it needs the new agent file present to review anything meaningful), so the source commit (agent, test, README) was made first and this journal — which needed the dry run's real output — was committed as a second commit on the same branch/PR, rather than bundled into one commit as the brief's Step 6 literally shows. |
| 2026-09-24 | Task 03: auth-core — package, tokens, secret cipher | subagent (sonnet) | 15:02 | 15:35 | one fix-round commit after the `security-reviewer` pass (verdict `FIX BEFORE MERGE`: 1 high, 5 medium, 3 low) | n/a (no critic pass scheduled for this task; the `security-reviewer` table is recorded in the Notes column instead) | TDD RED confirmed first (`pnpm turbo run build --filter=@tms/auth-core` failed with `TS18003: No inputs were found` before `src/` existed; then `pnpm turbo run test` failed all 3 suites — `purity.spec.ts` on the missing `src` directory, `tokens.spec.ts`/`secret-cipher.spec.ts` on `Cannot find module '../src'`), then GREEN after implementing `random.ts`, `tokens.ts`, `secret-cipher.ts`, `index.ts` verbatim from the plan (24/24 Jest tests). `lint`, `typecheck`, `build` each green; `dist/index.js` and `dist/index.d.ts` present. Lint probe (`@nestjs/common`, `@prisma/client`, `@tms/contracts` in a throwaway `src/lint-probe.ts`) gave exit=1 with exactly 3 `no-restricted-imports` errors and no extra boundaries error; removing the probe file gave exit=0. No deviations from the plan's literal file contents: the `packages.auth-core` boundaries entry and policy (`packages/config/eslint/base.mjs`) were already wired by the phase 1 stack, so the first commit only added the package itself. The `security-reviewer` agent was not registered as a directly invocable subagent in this session despite being merged in Task 02 (`Agent type 'ethernal-nest-react:security-reviewer' not found`), so it was run manually by handing its full checklist and persona (`tools/claude-plugin/ethernal-nest-react/agents/security-reviewer.md`) to a general-purpose agent instructed to follow it verbatim — a fallback worth fixing (plugin/session reload) before the next auth PR. Findings table: (1) HIGH — GCM AAD did not bind the envelope's own version/key id, so an envelope could in principle be relabelled to a different key id without failing authentication if that id's bytes matched (`secret-cipher.ts`); (2) MEDIUM — an injected `RandomSource` returning too few bytes silently produced a short IV or token with no length check; (3) MEDIUM — key material was visible via `util.inspect`/`JSON.stringify` of the cipher instance (TS `private` is not a runtime private field); (4) MEDIUM — an invalid `activeKeyId` or a hostile envelope key id could be echoed into the thrown error message; (5) MEDIUM — an empty AAD was accepted, binding a secret to no owner; (6) MEDIUM — no negative tests for a truncated tag/IV, wrong version or wrong part count (Node accepts short GCM tags unless `authTagLength` is pinned); (7) LOW — `safeEqual` branched on length before comparing (a timing signal) and, hashed as UTF-8, two different lone surrogates could compare equal; (8) LOW — `parseKeyring`'s plain `{}` accumulator let a `__proto__` key id write the prototype instead of an own property; (9) LOW — base64 decoding was lenient (deferred). Fixed 1, 3, 4, 5, 6, 7, 8 and the length-check half of 2 in one fix-round commit (13 new negative/property tests, 37/37 green); kept the injectable `RandomSource` seam itself (2's other half) because it is the mandated port per spec section 3 and the shared-interfaces table, and deferred 9 (stricter canonical base64 validation) as a follow-up — the auth tag already fails closed on any corrupted ciphertext, and tightening decoding risks rejecting valid keys from tools with different base64 dialects. |
