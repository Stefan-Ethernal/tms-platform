# Efficiency journal: critical-path lane

One entry per task. Fill in as you go, not afterwards. Times are local (Europe/Belgrade).
Approach values: `inline` (Claude in main session), `subagent` (dispatched agent), `skill`
(named skill), `parallel-session` (separate worktree + session), `human`.

| Date | Task | Approach | Start | End | Rework | Critic findings (total / accepted / prevented rework) | Notes |
|---|---|---|---|---|---|---|---|
| 2026-09-23 | Analyse 4 client documents (123-page functional description, architecture drawing, roadmap, proposal deck) | inline (pdftotext + targeted reads) | 08:15 | 08:40 | none | n/a | Extracting text to scratchpad then reading only relevant sections kept context small. |
| 2026-09-23 | Requirements interview (brainstorming skill, 3 rounds of grouped questions + 3 design rounds) | skill: superpowers:brainstorming | 08:40 | 09:35 | none | n/a | Grouping 2-4 related questions per round worked well; user added requirements mid-turn twice (PR verification, parallelisation). |
| 2026-09-23 | Verify Claude Code facts (chrome flag, plugin layout, hooks, best-practices doc) | subagent: claude-code-guide | 09:00 | 09:01 | none | n/a | Ran in background during interview; avoided guessing. |
| 2026-09-23 | Critic pass over spec (Disney "critic") | subagent: Plan (read-only, fresh context) | 09:40 | 09:46 | n/a | 25 / 23 full + 2 partial / est. 7 HIGH would have caused rework (TOTP brute force, admin without MFA, sequence race, unique orderId, tx boundaries, phase order, scope) | Strong signal the critic pass pays for itself at spec level. |
| 2026-09-23 | Incorporate critic + user review comments, finalise plan | inline | 09:46 | 10:05 | plan rewritten once (full rewrite cheaper than 25 edits) | n/a | Plan approved 10:05. |
| 2026-09-23 | Repo hygiene (move client docs, .gitignore, git init), spec written to repo, memory saved | inline | 10:06 | 10:10 | none | n/a | Spec written in English with the client anonymised because the repo is public. |
| 2026-09-23 | Create public GitHub repo, squash-only merges, ruleset on main (PR required, no force push/deletion, linear history) | inline (gh CLI) | 10:14 | 10:18 | none | n/a | Required status checks to be added to the ruleset once CI workflow job names exist (phase 0). |
| 2026-09-23 | Phase 0 implementation plan (13 tasks, `docs/superpowers/plans/2026-09-23-phase-0-bootstrap.md`) | skill: superpowers:writing-plans (+ claude-code-guide subagent for plugin/hook formats) | 10:20 | 11:05 | self-review fixed 5 issues before the critic (TDD order, e2e `test` script, Prisma env in CI, nested fences, tsconfig includes) | n/a (critic pass follows) | Fact-finding before writing (npm view, official Nest 12 / Vite 8 / Prisma 7 templates in scratch, ESLint 10 config lookup test, gitleaks checksum flow) corrected 6 assumptions that would have broken tasks: TS 7 unsupported by Nest CLI and typescript-eslint, Prisma `latest` is an 8 rc, pnpm 12 `allowBuilds`, Nest 12 ESM + Jest `--experimental-vm-modules`. Two "facts" from the guide subagent (no non-blocking Stop output, directory marketplace undocumented) were wrong and caught by the critic. |
| 2026-09-23 | Critic pass over the phase 0 plan (Disney "critic") | subagent: Plan (read-only, fresh context) | 11:05 | 11:12 | n/a | 24 / 24 / est. 7 (3 HIGH: eager Prisma `env()` breaking CI, no guard for the client name, missing root eslint config; plus 4 MEDIUM that would have failed a task's own acceptance step) | The critic verified facts read-only (tarballs, `npm view`, docs) and overturned two claims the guide subagent had made. Second consecutive critic pass that paid for itself; keep it mandatory per phase. |
| 2026-09-23 | Incorporate the 24 critic findings into the plan | inline | 11:12 | 11:45 | Tasks 5 and 10 split (5a/5b, 10a/10b); 3 facts corrected | n/a | Targeted edits (about 40) were cheaper than a rewrite this time because findings were local; the spec-level pass needed a rewrite. |
| 2026-09-23 | Task 1: workspace root + `@tms/config` (tsconfig/eslint/jest/vitest presets) | subagent (sonnet) | 11:44 | 11:58 | Two brief snippets did not pass their own acceptance tests as written and needed a minimal fix: `no-restricted-imports` used one glob `group` with a bare `@tms/domain` entry plus negated subpaths — gitignore-style matching (the `ignore` package) cannot re-include a child once a parent is excluded, so the bare specifier had to move to a separate `paths` ban, decoupled from the `patterns` group; and `eslint-plugin-boundaries` v7 could not resolve extensionless relative `.ts` imports at all (silently reported no dependency) until `settings['import/resolver'].node.extensions` included `.ts`/`.tsx` — its default resolver (`eslint-import-resolver-node`) only tries `.js/.json/.node`. Also added a package-local ignore for `test/fixtures/**` in `packages/config/eslint.config.mjs` so `eslint .` does not try to type-check the boundaries fixtures (they are fed to ESLint in-memory by the test, not part of this package's TS project). No extra `allowBuilds` entries were needed; the brief's list (esbuild, prisma, @prisma/engines, msw) was sufficient — `pnpm install` produced no `ERR_PNPM_IGNORED_BUILDS`. `pnpm install` also auto-added a `minimumReleaseAgeExclude` block to `pnpm-workspace.yaml` for freshly-published catalog packages (pnpm 12 default minimum-release-age gate); left as pnpm produced it. |
| 2026-09-23 | Task 2: NestJS API skeletons (`api-admin`, `api-driver`) | subagent (sonnet) | 12:07 | 12:13 | Primary Jest config (ts-jest, no `useESM`) worked unmodified, so the documented ESM fallback was not needed. The brief's own e2e-spec assignment (`configureApp(moduleRef.createNestApplication())`) failed `no-unsafe-assignment` because `configureApp`'s fixed signature returns untemplated `INestApplication` (`= any`); fixed with an `as INestApplication<App>` cast, no behavior change. `pnpm --filter a --filter b lint typecheck` does not run two scripts (pnpm passes `typecheck` as an arg to `lint`), so lint and typecheck were run as two separate commands. | n/a | `ExperimentalWarning` appeared only once the e2e-spec (not the plain unit spec) loaded; silenced via `NODE_OPTIONS='--experimental-vm-modules --no-warnings=ExperimentalWarning'` per the brief's allowance. Also had to set `@parcel/watcher`/`unrs-resolver` to `true` in `allowBuilds` (pnpm auto-added them as unresolved placeholders on `pnpm install`). |
| 2026-09-23 | Task 3: Vite SPA skeletons with MSW (`web-admin`, `web-driver`) | subagent (sonnet) | 12:22 | 12:32 | Brief listed the render test only under `web-admin`, but Step 4 expects "2 passed per app", so `App.test.tsx` was mirrored to `web-driver` verbatim (implied by "mirrors everything"). `web-driver`'s `package.json` description was given a driver-specific value ("Kiosk SPA (driver check-in)...") for consistency with `api-driver`'s own deviation in Task 2, since the brief only spelled out the admin string. | n/a | `pnpm install` needed no new `allowBuilds` entries. `msw init public --save` left `package.json`'s existing `msw.workerDirectory` field untouched (already matched); Prettier reflowed it to multi-line JSON, which is expected repo style. Dev-proxy check against the Task 2 API confirmed a JSON `{"statusCode":404,...}` body through the Vite proxy, and both `timeout`-wrapped background processes exited on their own. |
| 2026-09-23 | Task 4: Prisma `db` package skeleton (`packages/db`, no models) | subagent (sonnet) | 12:31 | 12:38 | `library.json` (unlike `nest.json`/`react-node.json`) sets no explicit `types`, so `@types/node` was never auto-picked up for `prisma.config.ts`'s `process.env` access, failing both `typecheck` (TS2591) and `lint` (`no-unsafe-assignment` on the error-typed value); fixed with a package-local `"types": ["node"]` in `packages/db/tsconfig.json`, matching the pattern already used by the other two presets. | n/a | Verified both without a database (`db:migrate:deploy` failed with Prisma's missing-datasource error, exit 1, as required) and against a throwaway `postgres:16-alpine` container (`migrate deploy` and `db:drift` both exit 0 on the empty schema). The "Update available 7.10.0 -> 8.0.0-rc" banner appeared once and was intermittent (Prisma's own update-check cache); `CHECKPOINT_DISABLE=1` is Prisma's documented env var to silence it but was not added since the brief's scripts don't call for it. |
| 2026-09-23 | Task 5a: docker-compose default profile (postgres, mailpit, migrate) and smoke test | subagent (sonnet) | 12:40 | 12:52 | The `migrate.Dockerfile` as given built and passed `compose config`, but the container exited 1 with Prisma's "please make sure you install prisma with the right permissions": the build stage lacked `openssl`, so the engine postinstall defaulted to `openssl-1.1.x`, then at runtime (openssl 3.0 installed, `USER node`, root-owned copied files) Prisma tried to re-fetch/write the correct engine and had no permission; fixed by installing `openssl`/`ca-certificates` in the build stage too, matching the runtime stage, so the correct engine is fetched at build time. | n/a | Verified end to end: `compose config -q` clean, `up -d --build` (~53s cold, ~8s cached), `smoke.sh` all green, negative check (wrong password) exited 1 with Prisma's P1000 auth error as required, then restored the stack. `pnpm --filter @tms/db deploy --legacy` worked as written, no pnpm-workspace.yaml change needed. Final `migrate` image: 760MB. |
| 2026-09-23 | Task 5b: docker-compose `full` profile — API images, SPA images behind Caddy | subagent (sonnet) | 12:53 | 13:05 | Initial round: none — `api.Dockerfile`, `web.Dockerfile` and `Caddyfile` built and ran unmodified from the brief; `pnpm --filter X deploy --legacy --prod /out` was accepted by pnpm 12.5.1 as given, so the `injectWorkspacePackages` fallback was not needed; the API skeletons have no Prisma dependency, so the Task 5a `openssl` lesson did not apply. Fix round 1 (14:40–14:55, task review, Important): the brief's `handle /api/*` matcher does not match the bare `/api` path (no trailing slash) — Caddy fell through to the SPA handler and served `index.html` instead of the API's JSON 404, violating D11. Fixed by widening the matcher; `handle /api /api/*` (the brief's shorthand form) is rejected by Caddy at startup ("wrong argument count... after '/api/*'" — the `handle` shorthand only accepts one path token), so used a named matcher instead: `@api path /api /api/*` + `handle @api { ... }` on both site blocks. Added a regression check to `infra/smoke.sh --full` (bare `/api` on both origins must return 404 + `application/json`). | n/a | Verified end to end: `compose config -q` (with `--profile full`) clean, `up -d --build` (~77s cold), `infra/smoke.sh --full` all green (both origins serve their SPA title and forward `/api/does-not-exist` to a JSON 404). Additional deep-link check beyond the smoke script: `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/some/route` and the 8081 kiosk equivalent both returned `200` with the SPA's `index.html` (via Caddy `try_files`), confirming client-side routing survives a hard refresh. Final image sizes: `tms-api-admin` 364MB, `tms-api-driver` 364MB, `tms-caddy` 89.1MB. Fix round 1 re-verified: `smoke.sh --full` green including the new bare-`/api` checks, and `curl -D- http://localhost:8080/api` / `:8081/api` both show `HTTP/1.1 404 Not Found` + `Content-Type: application/json; charset=utf-8`. Full stack left running for Task 6. |
| 2026-09-23 | Task 6: Playwright skeleton | subagent (haiku) | 12:51 | 13:10 | Fix round 1: Review found duplicated test logic. Created `e2e/tests/support/smoke.ts` with `registerSmokeSuite({ name, title, api })` helper that registers three parameterized smoke tests per project. Updated each project's `smoke.spec.ts` to just call the helper. API test now loops over both `/api` and `/api/does-not-exist` paths in one test. Fixed ESLint import resolution by adding `.js` extension. | n/a | Created e2e workspace with Playwright projects for web-admin and web-driver. All 6 smoke tests (3 per project) passed: SPA serves on correct origin, API forwards with JSON 404 for both paths, deep links work. Lint and typecheck clean. pnpm install auto-added no workspace.yaml entries. Stack remains running. |
| 2026-09-23 | Task 7: `@tms/scripts` hygiene check and pinned, checksum-verified gitleaks wrapper | subagent (sonnet) | 13:05 | 13:40 | TDD RED confirmed (`check-hygiene.mjs` missing, import error) before GREEN (13/13 vitest). The brief's own `.mjs`-only package did not hit the anticipated type-aware-parser gap (`disableTypeChecked` already covers `**/*.{js,mjs,cjs}` in the shared base config), so no package-local tsconfig fix was needed, contrary to the pre-flagged risk. Authoring slip while seeding the local, gitignored `forbidden-terms.txt`: its own comment prose used commas, and `parseForbiddenTerms` (per its own tested spec) splits on commas too, so comment fragments after a comma leaked through as bogus search terms and matched unrelated prose repo-wide; caught immediately by the brief's own clean-tree exit-0 check, fixed by rewriting the comment without commas — no script change. Fix round 1 (task review Important finding + controller ruling on my own comma concern): (1) `gitGrepForbidden` rethrew the raw `execFileSync` error on any git-grep failure other than exit 1 (e.g. fatal "not a git repository", exit 128), and that error's `.message` embeds every `-e <term>` argument verbatim; fixed to throw a sanitized `Error('git grep failed (exit N)')` with no `cause`. The new core ESLint rule `preserve-caught-error` then demanded a `cause` on that rethrow, directly conflicting with the fix's intent; resolved with a scoped `eslint-disable-next-line` plus a comment explaining why attaching `cause` would defeat it, rather than changing `packages/config`. (2) `parseForbiddenTerms` split on commas before dropping `#` lines, so a comment line containing a comma could still leak fragments (this was the root cause of my own earlier authoring slip); fixed to strip whole comment lines first, then split the remainder. Two new tests added, RED confirmed (both failed against the pre-fix code, reproducing the leak live) then GREEN: a non-git-repo `gitGrepForbidden` call with a dummy term asserts neither the thrown message nor `.cause` contain it; `parseForbiddenTerms('# names, one per line\nAcme')` → `['Acme']`. | n/a | Seeded the local terms file from `docs/client/` file names only (never opened the documents), excluding generic/product words per the controller ruling; verified each real term individually trips `--staged` with exactly one `file:line` line and no leaked text (4/4), and that comment lines correctly do not trip it. `gitleaks.sh` first run downloaded and checksum-verified 8.30.1 into `.cache/gitleaks/8.30.1/` (network available); repo scan and `gitleaks.test.sh` (3/3 `ok`, offline reuse + tampered-checksum rejection) both green. `pnpm install` added no new `pnpm-workspace.yaml` entries. Fix round 1 re-verified: 15/15 vitest + 3/3 gitleaks + eslint clean + hygiene exit 0 + `pnpm format:check` exit 0. |
| 2026-09-23 | Task 8: Git hooks (husky, lint-staged, commitlint) | subagent (haiku) | 13:18 | 13:24 | none | n/a | Created `commitlint.config.mjs`, `.husky/pre-commit`, `.husky/commit-msg`, `.husky/pre-push` as specified. All commitlint tests passed (bad message rejected, valid message accepted, long header rejected). ESLint passed on commitlint config. Pre-commit hook detected and rejected a seeded GitHub token via gitleaks (exit 1, "leaks found: 1"). Pre-commit hook formatted a demo file correctly and committed it successfully. Both demos cleaned up successfully. Hooks installed and verified via `git config core.hooksPath` = `.husky/_`. |
| 2026-09-23 | Task 10a: Claude Code plugin hooks (protect, format, verify reminder) | subagent (sonnet) | 13:25 | 13:50 | Fix round 1 (task review, Important): `classifyEdit`'s `.env`/`docs/client/` comparisons were case-sensitive, giving a false allow on case-varied paths (`DOCS/Client/x.pdf`, `.ENV`, `apps/api-admin/.ENV.LOCAL`, etc.) — a real risk on case-insensitive filesystems (macOS APFS, Windows/NTFS default) where such a path is the same file on disk as its lower-case form, so Claude could be routed around the hook. Fixed by folding to lower case only for the comparison (`base.toLowerCase()`, `rel.toLowerCase()` for the `docs/client/` prefix, `.env`/`.env.`/`.example` all folded), keeping the original `filePath` untouched in the reason string; `docs/clientele/` stays allowed in any case. Added 6 tests to `protect.test.mjs` (4 new blocked case-varied paths, 2 new allowed look-alikes `.Env.Example`/`DOCS/Clientele/x.pdf`); RED confirmed (4 failed as expected) before GREEN (61/61). | n/a | TDD RED confirmed (3 test files failed to import `lib/{protect,format,reminder}.mjs`; the 11 CLI tests failed because the hook scripts did not exist yet) before GREEN (55/55 vitest: protect 20, format 20, reminder 4, cli 11 — matches the brief's Step 4 breakdown exactly, not the plan overview's 54). All brief snippets worked unmodified, no lint fixes needed. `pnpm install` made no `pnpm-workspace.yaml` changes this run (`tools/*` already covered it). `claude plugin validate tools/claude-plugin/ethernal-nest-react --strict` passed clean (exit 0). Sanity-checked the package's own `validate` script beyond Step 4's ask: its first command (`claude plugin validate . --strict` against `tools/claude-plugin` itself) fails because that directory has no `.claude-plugin/marketplace.json` yet — expected, since Task 10b adds the marketplace manifest there; not a bug, left as specified. Fix round 1 re-verified: 61/61 vitest (protect 26, format 20, reminder 4, cli 11), eslint clean, `pnpm format:check` exit 0, and the package's own `validate` script now passes in full (Task 10b added the marketplace manifest in the meantime). |
| 2026-09-23 | Task 10b: plan-critic agent, verify/pr skills, marketplace, `.claude/settings.json` | subagent (sonnet) | 13:50 | 14:00 | Fix round 1 (task review, Important): `Bash(git branch:*)` was a prefix match that also allowed mutating subcommands (`-D`, `-m/-M`, `-f`) under a read-only-sounding name; narrowed to the exact-match `Bash(git branch --show-current)` in `.claude/settings.json`, no other entries touched | n/a | `CLAUDE_BIN=~/.local/bin/claude pnpm --filter @tms/claude-plugin validate` passed clean, exit 0 (marketplace then plugin, both "Validation passed"), no `$schema`/frontmatter issues so nothing needed to be removed. Load check: `~/.local/bin/claude -p "List the plugins..."` in a fresh nested session reported `ethernal-nest-react@tms` **enabled in `.claude/settings.json` but not loaded** (not present in `installed_plugins.json` — needs interactive trust/installation of the project marketplace, as anticipated by the brief's fallback). Per the brief's resolution, re-ran with `--plugin-dir tools/claude-plugin/ethernal-nest-react`: plugin loaded correctly and named all three hooks (`protect-files` PreToolUse, `format-on-edit` PostToolUse, `verify-reminder` Stop), the `plan-critic` agent, and both `pr`/`verify` skills — proves the plugin itself is sound; left the marketplace `path` relative (not changed to absolute, per instruction) and `pnpm claude` remains the documented interactive-session fallback. Manual hook check, 3 nested `-p` sessions (`--plugin-dir ... --allowedTools Edit`), each restored with `git checkout --` immediately after and confirmed via `git status --short`: (1) append comment to `apps/api-admin/.env.example` — **allowed**, edit applied (verified `grep`), nested session's own Stop hook additionally fired a `pnpm verify` reminder and it ran clean; (2) append line to `docs/client/README.md` — **blocked**, exact reason returned: "File is in a directory that is denied by your permission settings" (the settings.json `Edit(/docs/client/**)` deny rule fired ahead of the plugin's own hook, confirming the brief's "always-on layer" design; file untouched, `git status --short docs/client/` empty); (3) bad spacing added to `apps/api-driver/src/app.ts` (`demoHookCheck   =    1`) — **formatted after edit**, verified via `grep`: `export const demoHookCheck = 1;` (single spaces). All three restored; working tree clean apart from this task's new files at each check. |
| 2026-09-23 | Task 11: Documentation (CLAUDE.md, README, PR template, architecture, ADRs) | subagent (haiku) | 13:53 | 14:05 | Fix round 1 (task review, Important): Task entry misreported CLAUDE.md line count as 130; corrected to 61 (confirmed by `wc -l CLAUDE.md` output in Step 5 and commit verification). Also noted that CI job names were taken from the phase 0 plan, not verified against .github/workflows/ (Task 9 has not been written yet). | n/a | Facts verified against the real repository (root scripts, docker-compose profiles, plugin location) — all matched the brief exactly, no deviations. Created 8 documentation files: CLAUDE.md (61 lines, within 150-line limit), README.md, .github/pull_request_template.md, docs/architecture.md, docs/adr/{README.md,0000-template.md,0001-two-nest-processes-shared-packages.md,0002-prisma-orm.md}. All files use the exact text from the brief without modification. Created .github directory. Verified Step 5 checks (`node tools/scripts/check-hygiene.mjs && pnpm format:check && wc -l CLAUDE.md`), formatted all files, and verified commit preconditions. |
| 2026-09-23 | Task 9a: client name by convention (decision C), forbidden-terms check removed, ADR 0007 | subagent | 14:18 | 14:22 | none | n/a | Removed `FORBIDDEN_TERMS_FILE`, `parseForbiddenTerms`, `gitGrepForbidden`, the `forbiddenHits` input of `runHygiene` (now `runHygiene({ trackedFiles, claudeMd })`) and the `--staged` flag from `tools/scripts/check-hygiene.mjs`; the client-type document check and CLAUDE.md length check are unchanged. `.husky/pre-commit` no longer passes `--staged` to the hygiene script (gitleaks keeps its own `--staged`, a different flag). `check-hygiene.test.mjs` dropped the `parseForbiddenTerms`/`gitGrepForbidden` describe blocks, the "reports forbidden-term hits by location only" test and the now-unused `tempRepo()` helper: 15 tests before, 7 after (all green), plus 3/3 gitleaks tests unchanged. Documented the decision in `docs/adr/0007-client-name-by-convention.md` (option C, table of A/B/C reproduced) and cross-referenced it from `CLAUDE.md`, `README.md` (dropped the `forbidden-terms.txt` prerequisite bullet) and `docs/adr/README.md`. `node tools/scripts/check-hygiene.mjs` now prints `hygiene: ok (157 tracked files)`, exit 0, no warning line. |
| 2026-09-23 | Task 9: CI workflows (`ci.yml`: verify/hygiene/db-drift, `e2e.yml`) and Dependabot | subagent | 14:22 | 14:39 | none (all four checks green on push 1) | n/a | All three files (`ci.yml`, `e2e.yml`, `dependabot.yml`) written verbatim from the brief, no deviations. `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest -color` clean (exit 0, no output). Dependabot docker-ecosystem finding (corrected — see Task 9 fix round 1): dependabot-core's docker file fetcher matches candidate filenames against `DOCKER_REGEXP = /dockerfile|containerfile/i` (`docker/lib/dependabot/docker/file_fetcher.rb:12`, current default branch), an unanchored case-insensitive substring match, so `infra/docker/api.Dockerfile`, `migrate.Dockerfile` and `web.Dockerfile` are all discovered as-is under `directory: /infra/docker`; no gap, no rename needed. First-ever push of `feat/phase-0-bootstrap`: `.husky/pre-push` ran `turbo run typecheck test --filter='...[origin/main]'` against all 10 packages (14 tasks: 14 successful, 1 cache hit), clean. Both pre-publication gate commands (content diff and commit metadata against the terms list) printed 0 before the push. Opened draft PR #1 exactly per the controller's verbatim `gh pr create` command; both workflows triggered on `pull_request` and all four checks (`verify` 1m20s, `hygiene` 17s, `db-drift` 40s, `e2e` 1m55s — well under the 30-minute job timeout and the 15+ minute budget flagged as possible) passed on the first push, zero fix iterations. |
| 2026-09-23 | Final-review fix wave (11 items from the whole-branch review: 2 Important, 9 Minor) | subagent | 15:03 | 15:16 | none | n/a | The final review (a94fed5..cee6efc) found no Critical issues; it upgraded two deferred minors to Important (eslint-plugin-boundaries v5-era syntax under v7 printing five deprecation warnings on every lint; `gitleaks.test.sh` hardcoding `_x64`, red on arm64) and listed cheap hardening. Fixed: boundaries preset migrated to `boundaries/dependencies` + `policies` + entity selectors + `{{...}}` template, graph proven identical by a one-off 56-pair old-vs-new matrix (0 diffs), fixture test now also asserts the rendered message and zero `[boundaries]` warnings (RED 3 failed / GREEN 8/8); `gitleaks.sh` exposes its os/arch mapping via `GITLEAKS_PRINT_ASSET=1` and the test uses it (RED under an `aarch64` `uname` shim: "no checksum error message"; GREEN: `_arm64` asset, tamper assertion passes); smoke title names the project origin (6/6 e2e with distinct titles); `.superpowers/` in `.gitignore`/`.dockerignore` (redundant `.prettierignore` entry removed, Prettier reads `.gitignore`); four `.env.<environment>` deny entries; hygiene regex widened to 15 document extensions (RED 1 failed / GREEN 8/8); Postgres/Mailpit on 127.0.0.1 (LAN connect refused, Caddy still on all interfaces, `prisma migrate status` via `localhost` still works); both `main.tsx` log a failed MSW start and still render (new `main.test.tsx` per app, RED unhandled rejection + no mount / GREEN 3/3); PR-only `cancel-in-progress` and job timeouts (actionlint exit 0); formatter timeouts 2 × 25 s. `TURBO_FORCE=true pnpm verify` green (27/27 tasks, 0 cached), full stack rebuilt, `smoke.sh --full` and `pnpm e2e` 6/6. |
| 2026-09-23 | Task S1: split phase 0 into a stack of 10 small PR branches (local only, no push) | subagent | 15:27 | 15:47 | none | n/a | Why: the project owner rejected one ~16k-line PR (9.1k lockfile, 3.9k plan, ~3.75k code/docs) and chose 10 stacked PRs, one boundary each, opened at once and squash-merged in order. Branches, one commit each on `origin/main`: `phase-0/01-plan`, `02-workspace`, `03-api`, `04-web`, `05-db`, `06-compose`, `07-e2e`, `08-hygiene-hooks`, `09-claude-plugin`, `10-docs`. The shared root files (`package.json`, `pnpm-workspace.yaml`, `ci.yml`, `dependabot.yml`) were derived per branch from 3876f94 by removal only (a script whose branch-10 output is byte-identical to REF); each lockfile is REF's pruned by `pnpm install`, then `--frozen-lockfile`, with every `packages:` key present in REF (no drift, 02–10). Verified per branch 02–10: `pnpm install --frozen-lockfile` and `pnpm verify` (`TURBO_FORCE=true`); 05 `db:migrate:deploy` and `db:drift` against local postgres; 06 compose default and full profiles with `smoke.sh` / `--full`; 07 `pnpm e2e` 6/6 plus two screenshots; 08 hygiene, `@tms/scripts` tests, commitlint rejection; 09 plugin tests 61/61 and `claude plugin validate`. Final tree equals 3876f94 except `docs/client/README.md` (main's version kept, decision C) and this journal. No file moved between branches; `msw` joins `allowBuilds` in 02 (vitest's optional peer puts it in that graph) and `esbuild` (in no lockfile, REF included) arrives with Vite in 04. Lessons: pnpm checks the unpruned REF lockfile against the release-age policy before pruning, so the prune ran with REF's full `minimumReleaseAgeExclude` and the trimmed list was checked on a second install; turbo 2.11 writes a linked worktree's cache into the main checkout's `.turbo/cache` ("shared worktree cache") unless `TURBO_CACHE_DIR` points elsewhere. |
| 2026-09-23 | Chrome check (Task 12 Step 1) | inline | n/a | n/a | n/a | n/a | not run: Claude in Chrome extension not connected (nested claude --chrome -p loaded the tools; the extension reported not connected) |
| 2026-09-23 | Architect review, docs group (PR #2, #10) | subagent (opus) | 16:32 | 16:49 | none | n/a | Read-only, worktree `rev-docs`. #2: D2-1 (1 Minor, PR-body accuracy). #10: D10-1..D10-9 (2 Important, 7 Minor: journal Approach/times/inputs, CLAUDE.md, architecture.md, ADR index/0002, PR-template split). Reviews: github.com/Stefan-Ethernal/tms-platform/pull/2#pullrequestreview-5292576363, pull/10#pullrequestreview-5292574660. |
| 2026-09-23 | Architect review, workspace group (PR #11) | subagent (opus) | 16:32 | 16:52 | none | n/a | Read-only, worktree `rev-workspace` (+ `rev-workspace-top`). 3 Important (W1 held, W2, W3) + 6 Minor (W4, W5 held, W6 held, W7, W8, W9). Review: pull/11#pullrequestreview-5292616007. |
| 2026-09-23 | Architect review, apps group (PR #3, #4) | subagent (opus) | 16:32 | 16:55 | none | n/a | Read-only, worktree `rev-apps` + live compose stack. #3: A-1 (1 Important) + A-2..A-9 (8 Minor). #4: W-1 (1 Important) + W-2..W-6 (5 Minor). Reviews: pull/3#pullrequestreview-5292653559, pull/4#pullrequestreview-5292655352. |
| 2026-09-23 | Architect review, infra group (PR #5, #6, #7) | subagent (opus) | 16:32 | 16:57 | none | n/a | Read-only, worktree `rev-infra` + shared compose stack (project `tms`). #5: I5-1 (1 Important). #6: M6-1..M6-7 (7 Minor). #7: M7-1 (1 Important) + I7-1 (1 Minor). Reviews: pull/5#pullrequestreview-5292683349, pull/6#pullrequestreview-5292683660, pull/7#pullrequestreview-5292684753. |
| 2026-09-23 | Architect review, tooling group (PR #8, #9) | subagent (opus) | 16:32 | 16:58 | none | n/a | Read-only, worktree `rev-tooling`. #8: 8-I1..8-I3 (3 Important) + 8-M1..8-M3 (3 Minor). #9: 9-I1..9-I3 (3 Important, 9-I3 held) + 9-M1..9-M5 (5 Minor, 9-M1 held). Reviews: pull/8#pullrequestreview-5292696794, pull/9#pullrequestreview-5292698694. |
| 2026-09-23 | Arch fix wave F1 (branches 02-07, rebase 08-10) | subagent (opus) | 17:02 | 17:28 | none (DONE_WITH_CONCERNS) | n/a | 19 fix commits: W2 156a7ee, W3 5bba924, W4 ad9a80e, W7 41dd5c1, W8 f6fa790, W9 70d72ec (#11); A-1 23c0703, A-2 290b9fa, A-3 4f28d3c, A-5 6b18551 (#3); W-2 610a414, W-3 13a09f9, W-5 85559c6 (#4); I5-1 b4a6be5 (#5); M6-1 8c56068, M6-2 bb61fe4, M6-3 dae5533, M6-4 6a3bb78 (#6); M7-1 10111d2, I7-1 rename 12c5a93 (#7). Mutation proofs pass; forced verify green 02-10, lockfiles unchanged; pushed atomically with leases (new tips 02 70d72ec .. 07 12c5a93, rebased 08 83b78de, 09 5b394e3, 10 ab6b639); CI green on all; threads resolved #11 9/9, #3 7/7, #4 6/6, #5 1/1, #6 7/7, #7 2/2; bodies updated (15 Mermaid re-rendered). Concerns: stack top no longer equals REF 3876f94 (expected, #10 row updated below); commitlint `footer-leading-blank` warning on 2 commit messages (0 errors, left as is). |
| 2026-09-23 | Arch fix F2a, parallel (PR #8, branch `fix/08`) | subagent (sonnet) | 17:18 | 17:34 | none | n/a | 6 commits on `fix/08` (base a2575ec, worktree `scratchpad/fix-08`, ran in parallel with F1): 8-I1 04ebd48, 8-I2 73964d1, 8-I3 da31d7a (all Important, mutation-proof pass; gitleaks SHA-256 digests verified independently against the official v8.30.1 checksums.txt and a re-downloaded asset hash), 8-M1 b9fcef5, 8-M2 26e9d12, ADR 0007 wording df5e89e (8-M3 ruling). Forced verify 25/25, hygiene ok, gitleaks no leaks. No push/PR edits; worktree kept for the integrator. |
| 2026-09-23 | Arch fix F2b, parallel (PR #9, branch `fix/09`) | subagent (sonnet) | 17:18 | 17:34 | none | n/a | 7 commits on `fix/09` (base 9128330, worktree `scratchpad/fix-09`, ran in parallel with F1): 9-I1 bece109 (Important, dropped `git status/diff/log` allow entries past read-only, confirmed against Claude Code's own permissions docs), 9-M4 1fddca5, 9-I2 d4b768d (Important, hooks.json wiring test), 9-M3 d53b903, 9-M5 b0ac42f, 9-M2 11c87f2, and the `pr`-skill half of D10-9 922bed7 (Verification plan/results split). Mutation proofs pass for both Important findings. Forced verify 27/27, plugin tests 61 -> 69. No push/PR edits; worktree kept. |
| 2026-09-23 | Arch fix F2c, parallel (PR #2 body draft, PR #10, branch `fix/10`) | subagent (sonnet) | 17:18 | 17:32 | none | n/a | 9 commits on `fix/10` (base 8f1072d, worktree `scratchpad/fix-10`, ran in parallel with F1): D10-1 c5edbcf .. D10-9 9a9e04b (journal Approach/times/inputs, CLAUDE.md plugin-load wording, architecture.md diagram + dependency-rule scoping, ADR index/0002, PR-template Verification plan/results split). D2-1 drafted into `pr-body-01-d2.md` for the integrator to paste into PR #2 (PR body itself not touched). Forced verify 27/27, hygiene ok, gitleaks no leaks, Mermaid re-rendered 2/2. No push/PR edits; worktree kept. |
| 2026-09-23 | Integrator: land F2a/F2b/F2c on top of F1, publish | subagent (sonnet) | 17:32 | 17:44 | none | n/a | Cherry-picked `fix/08` (6 commits, 0 conflicts) onto F1's `08` tip 83b78de; rebased `09` `--onto` the new `08` tip and cherry-picked `fix/09` (7 commits, 0 conflicts); rebased `10` `--onto` the new `09` tip, cherry-picked `fix/10` (9 commits, 0 conflicts), then added this journal row. Every cherry-pick and rebase was conflict-free (each fix wave touched only its own branch's files). NEW08/NEW09/NEW10, verify tails, gate counts, pushed SHAs, PR-body updates, thread replies/resolutions and CI conclusions for all 10 PRs are recorded in `arch-integrate-state.md`. |
| 2026-09-23 | Phase 1 plan: exploration (phase 0 code state, plan conventions and ledger, library versions) | subagent: 3 × Explore (parallel, read-only) | 15:20 | 15:40 | none | n/a | Inputs from the phase 0 final review taken from `review-final-report.md` because the journal rows were not yet on `feat/phase-0-bootstrap`. Version facts that changed the plan: Prisma 7 requires a driver adapter and no longer runs seed/generate from `migrate dev`; `@sentry/nestjs` for Nest 12 is v11, published the same day; `@types/nodemailer` does not match nodemailer 10. |
| 2026-09-23 | Phase 1 plan: interview (2 rounds, 7 grouped questions) and design pass | inline + subagent: 2 × Plan (PR stack; toolchain risks) | 15:40 | 17:16 | none | n/a | Decisions: plan now / code after the phase 0 stack merges; `@tms/nest-bootstrap`; whole §7 schema in two PRs; generic seed data; INVITED admin only; sync as a pure function in `db`; MailSender port only; `predev` = deploy + drift + sync + seed (D12 wording). The toolchain critic-style pass found two facts that changed the design: dual-build deps (`@nestjs-cls/transactional`) would load twice if libraries were ESM → Nest libraries are CJS; `eslint-import-resolver-node` ignores `exports`, so boundaries silently skipped `@tms/*` imports → typescript resolver + specifier test. |
| 2026-09-23 | Phase 1 plan: spikes T1–T12 (module format, Prisma CJS client, deploy --no-optional, boundaries resolver, Testcontainers harness, pino-roll, nestjs-pino, Sentry 11, terminus) | subagent: 4 × general-purpose (parallel, scratch projects, `docker run` only) | 17:16 | 17:41 | none | n/a | All twelve defaults confirmed; five corrections fed into the plan: shared tsconfig presets must not set `rootDir`/`outDir` (TS6059 + stray emit), `nest-library.json` must not inherit `verbatimModuleSyntax`, Sentry 11 replaced `sendDefaultPii` with `dataCollection` and needs `enableRuntimeChannelInjection: false` under Jest, `app.close()` does not flush pino-roll (explicit `transport.end()` in a shutdown hook), terminus `HealthCheckService` cannot produce the D5 body (`HealthIndicatorService` can). Measured: API image tree 128 MB with `--no-optional` vs 357 MB; db deploy 329 MB vs 760 MB; harness run ~5 s. A bogus `DOCKER_HOST` does not fail Testcontainers (socket fallback), so the "no Docker" check became a unit test of the wrapped error. |
| 2026-09-23 | Phase 1 plan: writing (14 tasks, one PR each) | skill: superpowers:writing-plans (+ 6 writer subagents for task bodies, integrated and self-reviewed inline) | 17:20 | 06:42 (+1 d) | self-review folded into the reconciliation row below | n/a (critic pass follows) | Written in a worktree from `origin/main` (`phase-1/01-plan`), local only, while the phase 0 stack is being split in another session. |
| 2026-09-24 | Subagent model policy | decision (owner) | — | 05:56 | none | n/a | After a session-limit stop (seven parallel Fable subagents on 2026-09-23), every `Agent` call names its model: writers and implementers `opus`, spikes, Explore and mechanical checks `sonnet`; the owner names the critic model per pass (opus for the phase 1 plan). |
| 2026-09-24 | Phase 1 plan: reconcile all 14 tasks with the merged `main` (`cc47fec`; the phase 0 architect review changed 59 files) and the journal inputs, then the writing-plans self-review | subagent: 1 × general-purpose (opus, edits the task files in scratch) + inline header edits | 06:48 | 07:42 | Self-review fixes: Task 01's placeholder grep matched itself; `LOG_LEVELS` defined twice (logger owns it); duplicate `db:migrate:dev` turbo task; Task 07 fixture calls off Task 05's signatures; a nested Mermaid fence; trailing reconciliation blocks folded into Task 08 and Task 12 | n/a (critic pass follows) | Drift applied: `${configDir}` presets, existing preset/boundaries tests extended instead of created, `db:drift`, Dockerfiles/compose anchor, re-anchored doc inserts. Journal inputs added: `api`/`web` elements (W5), `HOST` via `listenHost` + a process-level `PORT` test (A-4, A-6), `{ status, service }` health body, restart policy and migrate-ordering proof (I7-1, M6-6); A-7/A-8 deferred to phase 2. Verified RED→GREEN in scratch: preset test, 9×9 boundaries matrix, hygiene. Not executed: cases needing Docker, Nest or the typescript resolver. |
| 2026-09-24 | Critic pass over the phase 1 plan (Disney "critic") | subagent: plan-critic (read-only, fresh context) | 07:48 | 08:30 | n/a | 17 / 17 / est. 8 (5 HIGH: gitleaks on two fixture passwords, parallel turbo writes to `src/generated`/`dist`, TS 7 `catalog:` in Nest packages, `engines`/`node:24` against `main`'s Node 26, harness `postgres:16` against compose 18; plus D7 regeneration without `DATABASE_URL` and CLAUDE.md anchors 10 lines low) | Two passes: pass 1 (9 findings) against `cc47fec`; pass 2 (8 findings) after `main` moved to `0d5f4ac` (Dependabot majors TS 7 + `nest-ts6`, Node 26, Postgres 18, `@types/node` 26), requested while pass 1 ran; the follow-up message only reached the agent after its report, so it had to be resumed. The critic verified in scratch: gitleaks on the plan, TypeScript 7.0.2 on the presets, `@testcontainers/postgresql` with PG 18. Third consecutive critic pass that paid for itself. |
| 2026-09-24 | Incorporate the critic findings into the plan | inline | 08:15 | 08:34 | Turbo test graph reworked (no `pretest`, `generate` split from `build`); RED expectations rewritten for transpile-only ts-jest; toolchain refreshed to `0d5f4ac`; Task 12 split recommended (12a/12b) | n/a | About 100 exact-string replacements via a script that refuses non-unique matches, plus one replace-all (8 `docker run` lines); branch fast-forwarded to `0d5f4ac`; Task 01 checks re-run: prettier ok, hygiene ok (173 files), placeholders 0, gitleaks no leaks, fences even, 14 tasks. |
| 2026-09-24 | Phase 3 change requests P1-1, P1-2, P1-3, P1-5 into Tasks 03–07 and 14 (P1-4 left to 3a; P1-6 via the drift critic pass) | subagent: 1 × general-purpose (opus) + owner decision | 08:22 | 08:30 | Two requested audit names renamed to pass the naming test (`admin.driver.pin-reset`, `admin.list.exported`); counts: Vitest 144 → 147, catalogue 31 → 33 codes, audit actions 48 → 50, `schema-operations.spec` 9 → 11 | n/a | Requests arrived by cross-session message from the phase 3 planning session; owner accepted four after a short cost analysis (shared files after 3a and D7 serial migrations vs YAGNI). Decisions file shared with the phase 3 session from `.phase1-wip/` (git-excluded). |
| 2026-09-24 | Task 02: Config presets, Jest interop, generate task and specifier-aware boundaries | subagent (opus) | 10:54 | 10:58 | Hygiene script written before its tests; reverted to `HEAD` so Step 8 got a real RED | n/a | RED 35 failed/78 passed → GREEN 113; hygiene 5 failed → 18 passed; apps 14 + 14 unchanged, lint 0 `[boundaries]`; resolver added without allowBuilds prompt (lockfile also gains `resolve-pkg-maps`). Toolchain drift on Node 26.10: presets (TS2591, TS1287, clean compiles) and the typescript resolver behave as spiked; `db:drift` on a database never touched by Prisma Migrate exits 1 (`not managed by Prisma Migrate`), so `db:migrate:deploy` runs first as in CI, then `db:drift` exit 0 (`No difference detected.`); `db:migrate:dev -- --name probe --create-only` pass-through shown (empty migration). Local `format:check` trips over the git-excluded `.phase1-wip/` (Prettier ignores `.git/info/exclude`). Times checked against the reflog: branch created 10:54:30, commit 10:59:10. Fix round 1 (11:05–11:07, review finding: pnpm keeps orphaned `.pnpm` directories after a version change, so the store listing reported false duplicates): the single-copy check reads the snapshot keys of `node_modules/.pnpm/lock.yaml` with a line reader (no new dependency); RED 7 failed/15 passed (the orphan CLI case exited 1) → GREEN 22 passed; real tree 933 snapshot keys, `hygiene: ok (180 tracked files, single copies ok)`. |
| 2026-09-24 | Task 03: `@tms/contracts` — enums, permission catalogue, seeded roles, audit actions and scrub list | subagent (opus) | 11:12 | 11:19 | Two comments reworded to drop plan task numbers (controller ruling); the brief's `grep 'Buffer'` dist check matched `ArrayBuffer`, re-run word-bounded (`\bBuffer\b`): no Node globals | n/a | 147 Vitest tests (5 files) incl. a 300-run fast-check redaction property; `require('@tms/contracts')` from CJS resolves through `exports`; `types: []` keeps Node globals out. RED 5 suites `Cannot find module` → GREEN 147 (16/18/5/11/97, as the brief counts); negative `process` check TS2591; CJS require 51 / 8 / 16 keys, `./enums` → `ERR_PACKAGE_PATH_NOT_EXPORTED`; forced uncached lint/typecheck/test/build 4/4; `pnpm verify` green except the known local `.phase1-wip/` format exception, hygiene run separately (ok, no leaks). No `minimumReleaseAgeExclude` entry for `fast-check@4.10.2`. Fix round 1 (11:21–11:28, review: `scrubString` value classes ate the backslash of `\"` in finished JSON log lines (invalid JSON, quoted secret kept), plain `key="v"`/`key='v'` unredacted, IdentityCard `serial` not a sensitive word, `Bearer [Filtered]` → `[REDACTED]]`): backslash excluded from the value classes, quoted and JSON-escaped value alternatives, `serial`/`serials` tokens; RED 10 failed/149 passed → GREEN 159 (scrub 97 → 109); scratch fuzz of 20 000 JSON lines valid and idempotent. Fix round 2 (11:30–11:39, re-review: a quoted value under a non-sensitive key sheltered inner pairs, e.g. `reason="bad pin=1234"`; a plain `"` after `key=` in JSON could span into the next string when that string began with a delimiter): non-sensitive quoted values are scrubbed recursively, and a plain `"…"` value may not start where a JSON string ends (`\s*[,:\]}]` next); the end-of-value lookaheads were dropped. The new committed fast-check property (JSON lines, 500 runs) also found a round-1 leak, `"password='X'"` as a JSON key. RED 9 failed/159 passed → GREEN 168 (scrub 109 → 118); property stress 20 000 runs green; 50k-character adversarial inputs linear, worst 6.1 ms (`a="` repeated; 2b4c40b 0.8 ms). Named groups replaced by positional ones after the first timing (7.8 ms). |
| 2026-09-24 | Task 04: Testcontainers harness (@tms/db/testing) | subagent (opus) | 11:44 | 11:53 | First `pnpm verify` failed one `@tms/config` preset test: pnpm's bin shims put `node_modules/.pnpm/node_modules` (every workspace package hoisted) on `NODE_PATH`, so with `@tms/db` now exporting a built `./testing/jest-global-setup` the "no dependency on @tms/db" case resolved the real package instead of throwing; the preset now checks the consumer manifest's (dev)dependencies before resolving, and the fixture consumer declares `@tms/db` (113 tests unchanged in number). The aborted run left a claude-plugin `tests/tmp/fmt-*.mjs` file behind (its `finally` never ran); removed | n/a | RED `Module <rootDir>/src/testing/global-setup.ts in the globalSetup option was not found` → GREEN 12 passed with `--maxWorkers=2`, template migrated once (1 log line), 2 clones in band (12 passed). Toolchain drift on Node 26.10 + `postgres:18-alpine`: behaves as spiked — container 2110 ms, `migrate deploy` 867 ms, 2 clones 78 ms, setup 3152 ms (full `pnpm verify`: 8 clones in 272 ms, setup 3523 ms); Jest 30 printed `PASS <file>` lines under turbo here. No-Docker run via `unshare -Urm` + tmpfs over `/run`: `Testcontainers could not reach Docker: Could not find a working container runtime strategy. Is the Docker daemon running?` in 0.82 s, exit 1. `TESTCONTAINERS_RYUK_DISABLED=true` through turbo: no labelled container afterwards; default run leaves only `testcontainers/ryuk:0.14.0` for its grace period; 0 containers after 12 s. allowBuilds ssh2/cpu-features/protobufjs=false, no other ignored build, no `minimumReleaseAgeExclude` line. Exports self-resolve (4 paths, 4 names); `db:validate` valid. `pnpm verify` 32/32 turbo tasks green except the known local `.phase1-wip/` format exception; Prettier without it clean, hygiene ok (205 files), no leaks. CI verify duration: after push |
| 2026-09-24 | Bump `db-drift` job's postgres image to 18-alpine to match `infra/docker-compose.yml` (Dependabot PR #15 bumped compose, left `ci.yml` on 16) | inline (worktree `tms-ci-pg18`, branch `ci/db-drift-postgres-18`) | 11:35 | 11:55 | none | n/a | One-line change (`.github/workflows/ci.yml` line 58); grepped repo for other `postgres:16` hits — only historical docs/plans/journal text, left untouched per instruction (fix CI only). Reproduced the job against a throwaway, uniquely-named `postgres:18-alpine` container on a free loopback port: `db:migrate:deploy` (no migrations yet, phase-1 territory) and `db:drift` both exit 0, "No difference detected". `pnpm install` + `pnpm verify` in the worktree: 27/27 tasks green, format/hygiene/gitleaks clean. Incident: checked the requested postgres-18 volume-layout risk by starting `infra/docker-compose.yml`'s `postgres` service from the worktree on an alternate port — compose's fixed `name: tms` meant this recreated the *main checkout's own running* `tms-postgres-1` (shared project across worktrees, same pattern noted in the architect-review infra-group row above), which then failed to start (exit 1) against the existing `tms_pgdata` volume: real evidence for the flagged risk — that volume predates the `/var/lib/postgresql` mount fix (commit 0d5f4ac, PR #14) and still holds pre-18 data at the old `.../data` subpath, so postgres 18's stricter `pg_ctlcluster`-style layout check refuses to start rather than silently losing data. Two follow-up attempts to restart the main checkout's postgres via `docker compose` (once via `cd`, once via explicit `-f`/`--project-directory`) were both denied by the Claude Code auto-mode classifier (Modify Shared Resources, then Interfere With Workloads) as touching a shared/main-checkout resource; left down and reported to the user with the one-line fix (`cd .../TankManagementSystem/infra && docker compose up -d postgres`) rather than working around the guard. Volume finding is real and reported as a separate-PR candidate, not fixed here. |
| 2026-09-24 | Phase 3a/02: plugin hooks pick the worktree that holds the edited file (9-M1) | inline (TDD) + skill: code-review | 08:12 | 08:30 | 1 fix round after `/code-review` (15 findings: 13 fixed, 2 deferred to the PR's risks) | n/a | Spike S7 reproduced the fail-open: a session started in the main checkout edits a sibling or `.claude/worktrees/` worktree, the root is `CLAUDE_PROJECT_DIR`, `path.relative` escapes with `../`, and `.env`/`docs/client` edits pass (exit 0). First fix (git toplevel as root) passed 82/82 but the review reproduced a regression: a symlinked path into a nested worktree now escaped, because git's physical toplevel met the tool's logical path. Final fix `locate(input, filePath)`: one `git rev-parse --path-format=absolute --show-toplevel --git-common-dir --show-prefix` on the nearest existing directory; the path is re-expressed under the toplevel, no root comparison. Also: `GIT_*` env stripped, 2 s git timeout, fixture signs nothing and cleans up. RED 7 → GREEN 84/84; mutation proofs: symlink handling (2 fail), `GIT_*` stripping (1 fail); `pnpm verify` exit 0; real-path S7 cases B/C/D and the reverse direction → exit 2, ~40 ms per hook call. |

| 2026-09-24 | Task 05: Schema — identity, access and audit | subagent | 12:01 | 12:10 | none | n/a | Migration `20260924100405_identity_access_audit` (6 types, 8 tables, 6 unique indexes, 7 indexes, 8 `ON DELETE RESTRICT`, 1 `ON DELETE CASCADE`); test counts 7 (enums-parity) + 7 (schema-identity), harness suites unchanged (12), total 26 passed; `db:drift` exit 0 (`No difference detected.`) after `db:migrate:dev` on a throwaway container it had itself just migrated. `importFileExtension = ""` confirmed on Node 26.10 + Prisma 7.10.0 (extensionless generated imports, tsc build produces `dist/generated/prisma/**/*.js`, no non-`.ts` file under `src/generated`). Prisma 7 driver-adapter error shape confirmed: `meta.driverAdapterError.cause.constraint.index` (no `meta.target`) on both P2002 and P2003. Deviation: `@tms/db#test` carries a Task 04 turbo override that also depends on its own `build` task, so the brief's expected RED (`$Enums` undefined at Jest runtime) surfaced instead as a `tsc` TS2305 build failure blocking the test task entirely — same missing-export root cause, confirmed separately with `pnpm turbo run typecheck --filter=@tms/db`. `pnpm verify` 33/33 turbo tasks green except the known local `.phase1-wip/` format exception; Prettier without it clean, hygiene ok (216 tracked files, single copies ok), no leaks. ERD rendered via `mmdc` (exit 0, 255714 bytes). |
| 2026-09-24 | Task 06: Schema — drivers, master data and operations | subagent | 12:30 | 12:50 | none | n/a | Migration `20260924104547_drivers_master_data_operations` (8 types, 9 tables, 9 unique indexes, 2 indexes, 12 `ON DELETE RESTRICT`, 0 `ON DELETE CASCADE`, all 12 `ALTER TABLE` statements confirmed to be new-table FK constraints only, no Task 05 table touched); test counts 15 (enums-parity) + 7 (schema-identity) + 11 (schema-operations), harness suites unchanged, total 45 passed; `db:drift` exit 0 (`No difference detected.`), `db:migrate:status` "Database schema is up to date!" (2 migrations found) after `db:migrate:deploy` + `db:migrate:dev` on a freshly recreated `tms-p1-pg`. `dist` smoke (`$Enums` keys) = 14. Same Task 04/05 deviation recurs: `@tms/db#test` depends on the package's own `build`, so a direct `pnpm run test` inside `packages/db` (bypassing turbo's build gate) was used to capture the brief's literal RED shape (20 failed/25 passed: 9 enum-parity + 11 schema-operations, all `Cannot read properties of undefined (reading 'create')` / length mismatches), separately confirmed via `pnpm turbo run test --filter=@tms/db` failing at the `tsc` build step (TS2305/TS2339/TS2694) for the same missing-export root cause. Both ERDs rendered via `mmdc` (exit 0, 255696 and 256372 bytes). `pnpm verify` 33/33 turbo tasks green except the known local `.phase1-wip/` format exception (pre-existing, git-excluded, untouched per the controller ruling); Prettier without it clean, hygiene ok (221 tracked files, single copies ok), no leaks. |
| 2026-09-24 | Task 07: Permission catalogue sync | subagent | 12:52 | 13:10 | none | n/a | Planner 14 table tests + 3 fast-check properties (300 runs), 8 database cases incl. two concurrent runs, CLI twice + wrong password (0 hits of the password in stderr); total `@tms/db` tests 45 → 73. ReadCommitted + advisory lock 7301 chosen over Serializable (snapshot before lock grant). `@tms/db#test` depends on the package's own `build` (turbo.json, already in place from Task 04), so the CLI specs run against a complete dist and nothing writes dist twice in parallel. `process.loadEnvFile` re-verified on Node 26.10.0: still never overrides an already-set variable, no drift. Manual CLI run against `tms-p1-pg`: first sync inserted all 33 codes, second sync empty, wrong-password run exit=1 with 0 hits of the password and 0-byte stdout (left `tms-p1-pg` running for Task 08 per the brief). Two lint/typecheck deviations from the brief's literal code, both from `Object.values` on an interface without an index signature resolving to the `any[]` overload under this repo's strict `@typescript-eslint/no-unsafe-*` rules: `isEmptyPlan`/`isEmptyReport` now cast to `Record<string, unknown[]>` before `Object.values`; the test's `withRename` helper casts `PERMISSIONS` elements (whose `as const satisfies` literal types omit `renamedFrom` when unset) to `PermissionDefinition` once instead of re-asserting the returned object, and a redundant `PERMISSIONS[0]!` became `PERMISSIONS[0]` (already a definite tuple element). The brief predicted `PrismaClientInitializationError` for the wrong-password case; Prisma 7.10.0 actually raises `PrismaClientKnownRequestError` with the same scrubbed, single-line message — behaviour matches the acceptance criterion, only the class name differs. `pnpm verify` 33/33 turbo tasks green except the known local `.phase1-wip/` format exception (pre-existing, git-excluded, untouched per the controller ruling); Prettier without it clean, hygiene ok (223 tracked files, single copies ok), no leaks. |
| 2026-09-24 | Task 08: Idempotent seed and the D12 predev flow | subagent | 13:14 | 13:35 | none | n/a | Seed matrix 9 tests (empty DB counts, exact grants per role, second run byte-identical snapshots, renamed/trimmed Operator kept, ACTIVE/BLOCKED admin, foreign email warning, missing email rolls back everything, two concurrent seeds → one admin); CLI 6 tests incl. drift probe → exit 2 with the hint. `@tms/db` test count 73 → 88 (9 seed + 6 CLI). `predev < /dev/null` run twice against a freshly created `tms-p1-pg` (controller ruling: this task's container starts from scratch, so run 1 applies both migrations and creates all rows, run 2 shows `No pending migrations to apply.` / `No difference detected.` and `rolesCreated: []`, `bootstrapAdmin: "exists"`), then `prisma db seed` through `migrations.seed` with and without `BOOTSTRAP_ADMIN_EMAIL` (both exit 0, admin already exists). Deviation 6 applied: no `migrate dev` in predev; `dev-setup.ts` runs `migrate deploy` then `migrate diff --exit-code` and maps exit 2 to the drift hint. One deviation from the brief's literal test code: the `seed(undefined)` case in `seed.spec.ts` cannot reach a literal `email: undefined` through the `seed` helper's own default parameter (JS resolves an explicit `undefined` argument the same as an omitted one, confirmed with `node -e`), so that one assertion now calls `seedDatabase` directly with `{ bootstrapAdmin: { email: undefined } }`; the acceptance criterion (`SeedConfigError`, zero rows written) is unchanged. `pnpm verify` split as Task 07 documents: `pnpm turbo run lint typecheck test build` 33/33 green, repo-wide `prettier --check .` fails only on the pre-existing, git-excluded `.phase1-wip/decisions-on-phase3-requests.md` (untouched per the controller ruling), scoped `prettier --check` on every file this task touched is clean, `pnpm hygiene` ok (234 tracked files, single copies ok), gitleaks no leaks. |
| 2026-09-24 | Task 09: Migrate image | subagent | 13:45 | 13:58 | none | n/a | `prisma` moved to `optionalDependencies` in `@tms/db`, `packageExtensions` added for the Task 14 adapter; `pnpm install` added one `packageExtensionsChecksum` line, no peer-dependency warning, `.bin/prisma` still linked, all 89 `@tms/db` tests green. Image = `pnpm --filter @tms/db deploy --legacy --prod /out` after `generate` + topological `build`; CMD `prisma migrate deploy --config ./prisma.config.ts && exec node dist/cli/seed.js` (sync runs inside the seed's own transaction, Task 08); root-owned `/app`, `USER node`, compose `read_only: true` + `tmpfs: [/tmp]`. RED: smoke against the phase 0 image failed exactly as predicted (`Cannot find module '/app/node_modules/@tms/contracts/dist/index.js'`, exit 1) on project `tms-p1` (alternate ports; the main checkout's own `tms` stack, `tms-postgres-1`/`tms-mailpit-1`, stayed untouched throughout — verified with `docker ps` before/after every compose call). Build: `Generated Prisma Client (7.10.0)`, `naming to docker.io/library/tms-p1-migrate`, exit 0. Sizes: image 830 088 648 B (phase 0 760 416 930 B), `/app` 336 368 323 B (phase 0 296 888 280 B; spike 321 318 274 B) — within the brief's 300–340 MB band, so the default 355 000 000 B CI budget applies unchanged. Contents check: no `typescript`/`eslint`, `.bin/prisma` present, 4 CLI files, `root`-owned `dist`/`node_modules`, `@prisma/{adapter-pg,client}` + `@tms/{contracts,node}`, `id -un` = `node` — all as predicted. `docker run` twice on `--read-only --tmpfs /tmp` against a fresh `tms-p1-pg`: run 1 `rolesCreated:["admin","operator","driver"]`/`bootstrapAdmin:"created"` exit 0, run 2 `rolesCreated:[]`/`bootstrapAdmin:"exists"` exit 0, no-email run against the existing admin exit 0, no-email run against a fresh `empty` database exit 2 with `SeedConfigError: BOOTSTRAP_ADMIN_EMAIL is required while no admin exists` — matches the brief verbatim. GREEN smoke on `tms-p1`: `33 active permissions (= catalogue), 3 system roles, 1 admin(s)`, idempotent rerun, mailpit reachable, `smoke: all checks passed`, exit 0; stack torn down (`down -v`) and `tms-p1-net`/`tms-p1-pg` removed after Step 4. CI budget step added to `.github/workflows/e2e.yml` (`MIGRATE_APP_MAX_BYTES=355000000`, image `tms-migrate`); local check against `tms-p1-migrate` confirms `within budget: 336368323`. `pnpm verify` split as Tasks 07–08 document: `pnpm turbo run lint typecheck test build` 33/33 green, repo-wide `prettier --check .` fails only on the pre-existing, git-excluded `.phase1-wip/decisions-on-phase3-requests.md` (untouched per the controller ruling), scoped `prettier --check` on every Prettier-applicable file this task touched is clean (the Dockerfile/`.env.example`/`smoke.sh` have no Prettier parser, checked instead with `bash -n`), `pnpm hygiene` ok (245 tracked files, single copies ok), gitleaks no leaks. |
| 2026-09-23 | Phase 2 plan: requirements interview and meta-plan (plan mode) | skill: superpowers:writing-plans (inline) + 2 subagents: Explore | 15:56 | 17:34 | none | n/a | Two Explore agents at 15:57 (phase 0 code patterns; phase 0 plan format and PR split) kept the main context small. Three grouped question rounds, 11 decisions: 15:58 (phase 1 dependency, phase 2 vs 3a scope, lockout, bootstrap), 16:35 (auth-core shape, Origin check, error format), 17:15 (permissions, locked state, stack A base, invite URL). Meta-plan (decisions, PR stack, design pins) approved 17:34. |
| 2026-09-23 | Phase 2 meta-plan stress test (PR stack) | subagent: Plan (fresh context) | 17:18 | 17:33 | none | 5 / 5 / est. 3 (counter reset, revocation ordering, stack A preset dependency would each have reopened a merged PR) | Gaps found: a third route marker (`@RequireSession`), account status checked per session scope, a correct password must not reset the failure counter, revocation ordering, stack A needs phase 1's presets. All five adopted as "design pins" in the meta-plan before approval. |
| 2026-09-23 | Phase 2 spikes A (auth-core libraries) and B (Nest and infra), first attempt | subagent (general-purpose, model not set), 2 in parallel | 17:36 | 17:47 | both agents failed on the session limit; no output survived | n/a | Plan writing continued inline meanwhile (head, overview, Tasks 01–02, 07–10 at 17:39–17:47). Lesson: time-box spikes in background agents and have each append its findings to a notes file as it goes, so a failed agent still leaves evidence; set `model` explicitly. |
| 2026-09-24 | Phase 2 spikes A and B rerun, plus S3b (zxcvbn tuning) | subagent (sonnet), 2 in parallel | 05:53 | 06:15 | facts corrected in parts already written: argon2 exposes its options as a const enum and has a native `secret` input (used as the pepper); throttler's default key is per route and a 429 carries `retry-after-<name>` headers; zxcvbn costs 200–400 ms on repeated patterns (tuned to `l33tMaxSubstitutions: 16, maxLength: 64`, run only after authentication or a valid token); `emit` vs `emitAsync` hazard for after-commit mail; `__Host-` cookies fail on a LAN IP or hosts alias | n/a | Spike notes were working files and are not committed; every fact the tasks rely on is restated in the plan's Tech Stack and task notes. |
| 2026-09-24 | Phase 2 plan: Tasks 03–06 and 11–19 | inline (skill: superpowers:writing-plans) | 05:56 | 06:16 | none | n/a | Written as separate part files while the spike reports came in; handoff file written at 06:16 before `/clear`. |
| 2026-09-24 | Phase 2 plan: Tasks 20–23, tail, assembly, check against phase 0 on `main` | inline + subagent: Explore (sonnet) | 06:18 | 06:43 | Explore check against phase 0 on `main` led to six corrections: Task 02 (six existing plugin test files; clean-tree check), Task 14 (new `[allowlist]` in `.gitleaks.toml`), Task 15 (create `apps/api-admin/turbo.json`), ADR index is prose, journal has a "Critic findings" column, stack A waits only for `phase-1/02-config` | n/a | Assembled plan: 8411 lines, 23 tasks, placeholder grep clean. New design choices for the critic: `FOR NO KEY UPDATE` with one lock order, admin actions never target oneself, a keyring instead of a single encryption key. |
| 2026-09-24 | Phase 2 plan follow-up to PR #14 (TypeScript 7, Node 26, Postgres 18) | inline | 07:40 | 07:48 | parts edited: Tech Stack, Task 03 (`"typescript": "catalog:nest-ts6"`), Task 04 (`node:26-bookworm-slim`), throwaway `postgres:18-alpine` | n/a | auth-core spike re-run on Node 26.10.0: 5 suites / 13 tests green, `tsc --noEmit` clean. |
| 2026-09-24 | Phase 2 plan: phase 3 requests P2-1..P2-7 | inline | 08:00 | 08:20 | parts edited: Tasks 08, 09, 11, 13, 20, 22, 23 | n/a | Requests came from the phase 3 plan session as a file. Applied P2-1 (driver targets → 409), P2-3 (global no-store), P2-4 (Prisma constraint mapping, P2003 → 409), P2-5 (phase 3a "allowed" wording); P2-2 declined by the project owner (phase 3a splits the snapshot per host module); P2-6/P2-7 needed no change. Plan now 8547 lines. |
| 2026-09-24 | Phase 2 plan: fold two journal inputs into tasks (A-7 helmet in Task 09; D10-6 auth-core `no-restricted-imports` in Task 03, struck in Task 23) | inline | 08:28 | 08:38 | none | n/a | Found while writing the journal rows: both inputs were listed for phase 2 but no task covered them. helmet 8.3.0 checked for a CJS entry and bundled types before it went into the plan. |
| 2026-09-24 | Phase 2 plan: reconcile with the committed phase 1 plan (`96556f9`) | subagent (opus), read-only | 08:40 | 11:08 | The run died at 08:42 on the session limit (reset 10:50); a reboot then wiped the scratchpad with the phase 1 copy and the agent's notes. Resumed at 10:53 from its transcript, it rebuilt the notes from its context and finished | n/a | 27 differences, of which 7 would not have compiled or would have failed phase 1's own tests (audit action names and count, `tokensRevoked` as a sensitive key, whole-word scrub matching, `SessionScopeSchema`, `withAdminClient` on the maintenance database, the env schema's location, phase 1 tests pinning the old error bodies). Every deferral from phase 1 to phase 2 was checked against a task. |
| 2026-09-24 | Phase 2 plan: critic pass | subagent: `ethernal-nest-react:plan-critic` (Fable), read-only, in parallel with the reconcile | 08:40 | 11:06 | same session-limit loss and resume as the reconcile row | 23 / 22 (1 rejected: C2 contradicted phase 1, where the public marker sits on the handler) / est. 3–4 engineer-days | Highest: a 423 after a correct password on a locked account let an attacker keep guessing through the lock. Also: a password reset silently voided a pending 2FA reset; the bootstrap CLI re-invited a second admin on every `pnpm dev`; an absolute-expiry test that never reached the expiry; an Origin test on a route where guards never run. |
| 2026-09-24 | Phase 2 plan: triage and owner decisions | inline + AskUserQuestion | 11:06 | 11:20 | none | n/a | Four decisions from the project owner: generic 401 while locked on public routes; a password reset re-issues a pending MFA_RESET link; a sole-admin recovery CLI in phase 2 (placed in Task 22, where `MfaResetService` exists); stack A also waits for `phase-1/03-contracts`. |
| 2026-09-24 | Phase 2 plan: apply reconcile and critic fixes | 5 subagents (opus) on disjoint part files, one shared change spec with canonical names | 11:21 | 11:38 | none | n/a | The change spec (canonical audit-action table, env and transaction-host names, owner decisions, per-writer assignment) kept five parallel writers consistent: the cross-part pass found 4 small leftovers (an export sort note, a boundaries attribution, a task reference, an overview count). Writers also found phase 1 facts beyond the reports (`RequestContext` lives in CLS; `configureApp` needs a logger; an invite FAILURE audit row rolled back with its own transaction). Plan 8598 → 9665 lines. |
| 2026-09-24 | Phase 2 plan: consistency pass, assembly, commit | inline | 11:38 | 11:52 | none | n/a | Old-name grep over all parts; every audit action used in the plan exists in phase 1 or among the 5 new keys; commit headers ≤ 100 characters; prettier clean; placeholder grep clean; gitleaks flagged three test values in the plan (RFC 6238 vector, a fixture password, the public CI pepper), which now carry `gitleaks:allow` like phase 1's fixtures (the worktree has no husky hooks, so hygiene and gitleaks were run by hand). Lessons: background agents died twice on the session limit (spikes on 09-23, reconcile and critic today) — incremental notes files plus resuming from the transcript saved the work the second time; keep notes outside the scratchpad, which a reboot wipes. |
| 2026-09-24 | Task 10: @tms/logger (redaction, request id, rolling files) | subagent | 14:05 | 14:35 | none | n/a | 42 tests (27 pure + 15 with pino/Nest/pino-roll), detectOpenHandles clean, dist smoke file name; spike facts applied: formatters before serializers, streamWrite pass, one pino-http per process. RED 6 suites failed to load with the brief's exact `Cannot find module` messages → mid-point GREEN 27 passed/3 suites still missing → GREEN 42 passed, 6 suites, both JSON stdout lines (`destination probe`, `file probe`) printed raw as predicted. `--detectOpenHandles`: same 42/6, no open-handle block. Three comments copied from the brief cited plan artefacts (`Task 12`, `Task 14`, `spike S4` ×2) and were reworded to state the reason instead (controller ruling; grepped clean afterwards). One test bug in the brief's own `logger.e2e-spec.ts`: `/api/boom` returns 500, and pino-http's `onResFinished` (read from the installed `pino-http@11.0.0` source) always logs `"request errored"`, never `"request completed"`, once `res.statusCode >= 500` — the brief's `completed('/api/boom')` wait therefore times out; added a local `finished()` predicate accepting either terminal message, acceptance criterion (secrets absent from the finished line) unchanged. Two lint findings in `options.spec.ts` under this repo's `recommendedTypeChecked` preset (no test-file relaxation, no `eslint-disable` precedent in the repo): the brief's `{ url }` cast to `IncomingMessage` had been dropped by the write/format hook, leaving the import unused, restored verbatim; `expect(options.hooks).toEqual({ logMethod: expect.any(Function), ... })` tripped `no-unsafe-assignment` on the `any`-typed `expect.any(Function)` property, split into `expect(options.hooks?.streamWrite).toBe(scrubString)` + `expect(typeof options.hooks?.logMethod).toBe('function')` (same coverage). `pnpm verify` split as Tasks 07–09 document: `pnpm turbo run lint typecheck test build` 37/37 green (incl. `@tms/db`'s 89 Testcontainers tests), repo-wide `prettier --check .` fails only on the pre-existing, git-excluded `.phase1-wip/decisions-on-phase3-requests.md` (untouched per the controller ruling), scoped `prettier --write`/`--check` on every file this task touched reports "unchanged", `pnpm hygiene` ok (245 tracked files, single copies ok), gitleaks (`git --redact --no-banner`) no leaks. No `minimumReleaseAgeExclude` entry added for `nestjs-pino@5.2.0`/`pino@10.3.1`/`pino-http@11.0.0`/`pino-roll@4.0.0`, no `ERR_PNPM_IGNORED_BUILDS`. |
| 2026-09-24 | Task 11: @tms/nest-bootstrap, app refactor, api image builds dependencies | subagent | 15:11 | 15:35 | none | n/a | Env matrix 38 (exact zod messages, HOST defaults per NODE_ENV), CoreModule 4 + 2 (one app per spec file: nestjs-pino keeps one pino-http per process; level, request id, file off / on), bootstrapApi 2 (invalid env, one stderr line + exit 1, real listen on 127.0.0.1) + 2 in a real process (PORT=abc / PORT=0, exit 1, one line); apps 2 suites / 5 tests each, X-Request-Id on the 404, probe route and SIGTERM kept. PORT=abc exits 1 without a stack trace. Both images read-only, uid 1000, LOG_DIR volume with api-admin/ + api-driver/, LOG_FILE_ENABLED=false gives an empty volume, no Prisma CLI. smoke --full + Playwright 8/8 on tms-p1. ADR 0008. dev script loads .env via --exec. RED confirmed at every step exactly as the brief predicted (env.spec Cannot find module '../src/env'; then module/bootstrap specs Cannot find module '../src'/'../../src' plus both bootstrap.process cases failing on the missing dist/index.js; then both apps envSchema undefined / AppModule.forRoot is not a function). One lint fix beyond the brief: app?.getHttpServer().address() in bootstrap.spec.ts is any under getHttpServer()'s untyped return, tripping no-unsafe-call/no-unsafe-member-access; cast through node:http's Server type, same assertion. Two comments in the brief's own snippets (core.module.ts, env.ts) and one in the README cited task numbers ("Task 12", "Task 13"); reworded to state the reason instead (controller ruling), as was the Dockerfile's "Task 12"/"spike S1" comment and ADR 0008's "phase 1 plan deviations 2 and 8" line. pnpm verify: 41/41 turbo tasks green (incl. @tms/db's 89 Testcontainers tests), prettier --check . clean, hygiene ok (287 tracked files, single copies ok, one @nestjs/common + one @nestjs/core), gitleaks no leaks. |
| 2026-09-24 | Task 12a: `@tms/db/nest` (PrismaModule/PrismaService) and the in-process TCP fault proxy, first half of a two-PR split | subagent | 15:35 | 15:42 | none | n/a | Steps 1-4 only, as the brief's own header recommends; steps 5-16 (HealthModule, CoreModule wiring, compose healthchecks, migrate-ordering proof, docs) left for Task 12b. Step 1 added `@nestjs/terminus@12.1.0` to the catalog (no `minimumReleaseAgeExclude` needed, published 2026-09-20) and extended `packages/db/package.json` (`./nest` export, optional Nest peers, devDependencies), `packages/nest-bootstrap/package.json` (terminus, `@tms/contracts`, `@tms/db`, `@testcontainers/postgresql`) and both apps' `devDependencies` (`@tms/db`) even though the health module that needs them lands in 12b — shared groundwork the brief lists under this half; `pnpm install` exit 0, lockfile +99 lines, no peer warnings, no `ERR_PNPM_IGNORED_BUILDS`. RED confirmed exactly as predicted: `test/nest.spec.ts` failed to load with `Cannot find module '../src/nest'`, the 12 suites/89 tests from Tasks 04-08 unaffected. GREEN after writing `packages/db/src/nest/{index,prisma.service,prisma.module}.ts`: 13 suites/93 tests; `dist/nest/` and `dist/testing/fault-proxy.js` present with maps. Two doc comments copied from the brief's own snippets referenced "spike S3" and "Task 14"; reworded to state the reason instead (controller ruling) before committing, not caught by review. Full `pnpm verify`: 41/41 turbo tasks green, `prettier --check .` clean, hygiene ok (307 tracked files, single copies ok), gitleaks no leaks; `@tms/nest-bootstrap` stayed at 5 suites/48 tests and both apps at 2 suites/5 tests each, confirming no Step 5+ files were touched. No timing-sensitive test added by this half (the fault proxy itself is Step 2 scaffolding for 12b's recovery tests; the one timing assertion here, `connectTimeoutMs`, is the brief's own pool-layer bound test, unchanged from its text). |
| 2026-09-24 | Task 12b: health endpoint (`HealthModule`, compose healthchecks, migrate-ordering proof), second half of the two-PR split | subagent | 15:54 | 16:06 | none | n/a | Steps 5-16 on top of Task 12a's tip. RED confirmed exactly as predicted: `health.controller.spec.ts` and `health.e2e-spec.ts` failed to load with the brief's exact `Cannot find module` messages, `env-database.spec.ts` failed all 14 cases, Task 11's five suites passed unchanged. Mid-point GREEN after extending `baseEnvSchema` with `DATABASE_URL`/`HEALTH_DB_TIMEOUT_MS` and updating Task 11's five specs (env, core.module ×2, bootstrap, bootstrap.process) with the harness DB URL: 62/62. Full GREEN after writing `HealthModule`/`HealthController`/`HealthService`/`HealthModuleOptions` and wiring `PrismaModule.forRoot`+`HealthModule.forRoot` into `CoreModule`: `@tms/nest-bootstrap` 8 suites/71 tests (env-database 14, controller 3, health e2e 6, Task 11's 48 unchanged); both apps 2 suites/6 tests each (harness container ~3.5s). Images against a throwaway `tms-p1-pg`: ok → degraded (503 within ~1s of `docker stop`) → recovered without a restart, healthcheck exec exit 0/1, zero `/api/health` access lines, zero raw `tms:tms@` in logs, no Prisma CLI in the API image. Full `tms-p1` compose stack: `up -d` returned only once both APIs were `healthy` (Caddy's `service_healthy` wait), `restart: unless-stopped` on both, `smoke.sh --full` all-ok including the new migrate-ordering proof and per-origin `/api/health` checks, Playwright 10/10. One deviation from mechanical brief-copying: the post-write formatter hook silently dropped the `as unknown as HealthService` cast from the brief's own `health.controller.spec.ts` snippet on first write (a stale-format race, not a deliberate autofix), which then tripped `no-unused-vars` on the type import; caught by the Step 11 lint run and restored verbatim, no test or acceptance criterion changed. `pnpm verify`: 45/45 turbo tasks green (`@tms/db` 93, `@tms/nest-bootstrap` 71, `@tms/api-admin` 6, `@tms/api-driver` 6), `prettier --check .` clean, hygiene ok (325 tracked files, single copies ok), gitleaks no leaks. No plan-task-number or spike-label comments found in the new/edited files (checked before committing per the controller ruling). Every manual `tms-p1-pg`/`tms-p1-net`/`tms-p1-logs` resource and the full `tms-p1` compose project were torn down at the end of their steps. |
| 2026-09-24 | Task 13: Sentry, disabled without a DSN | inline | 16:08 | 16:34 | fix round 1 (review): narrowed the `supports-color` override to a `parent>child` selector; see task-13-report.md | n/a | 5 Sentry suites / 46 tests (options 14, scrub 21, env 6, capture 3 with a recording transport, disabled 2); `--detectOpenHandles` clean; Nest-free lint probe 1 error then 0; `dist/main.js` requires `./instrument` first; empty and unreachable DSN boot with 0 bytes on stderr; image `SENTRY_RELEASE` = short SHA (`dev` when `GIT_SHA` is omitted); real event: not run (no DSN provided). RED confirmed as predicted: `env.spec.ts` 4 failed/2 passed/6 total, the other four suites failing to compile on `Cannot find module '../../src/sentry'`. Real toolchain drift not in the spike (which used a smaller scratch project): adding `@sentry/nestjs` made pnpm resolve a second, distinct copy of `@nestjs/common`/`@nestjs/core` for `@tms/nest-bootstrap` alone — traced with `pnpm why` to `@sentry/node`'s runtime dependency on `@sentry/bundler-plugins`, which pulls `supports-color@^8` where the rest of the workspace is on `7.2.0`; the extra copy broke cross-module class identity and `capture.e2e-spec.ts` failed every case with `Nest can't resolve dependencies of the LoggerModule (pino-params, ?)` even before `SentryModule`/`SentryGlobalFilter` were wired in. Fixed by pinning `supports-color: 7.2.0` under a new `overrides:` key in `pnpm-workspace.yaml` (precedent: the existing `packageExtensions` entry for the same class of problem), which collapsed the graph back to one Nest copy; hygiene's single-copy check confirmed it (336 tracked files, single copies ok). No `minimumReleaseAgeExclude` entries were needed or added for `@sentry/*@11.0.0` (the store already had them cached and pnpm did not flag the release-age gate on this run, unlike the brief's spike). Manual verification: local process boot with empty/unreachable `SENTRY_DSN` (404 then 0 bytes on stderr, both iterations); two `docker build`/`run` pairs (`SENTRY_RELEASE` = short SHA with `GIT_SHA`, `dev` without); throwaway `tms-p1-pg` container, both DSN variants (`{"status":"ok","service":"api-admin"}`, 0 `[Sentry]` log lines), torn down after; full `tms-p1` compose stack (`up -d` waited for both APIs `healthy`, `printenv SENTRY_RELEASE` = short SHA on both, `smoke.sh --full` all-ok), torn down with `down -v`, built images removed. `pnpm verify`: 45/45 turbo tasks green (`@tms/nest-bootstrap` 13 suites/117 tests), `prettier --check .` clean, hygiene ok (336 tracked files, single copies ok), gitleaks no leaks. No plan-task-number or spike-label comments found in the new/edited files (checked before committing per the controller ruling). |
| 2026-09-24 | Task 14: `@tms/domain/shared` — transactions, audit, clock, mail port | subagent | 16:59 | 17:15 | one test-code fix (see notes) | n/a | 4 suites / 20 tests (audit 10 against Testcontainers: commit, rollback, fail-closed metadata, nested savepoint, autocommit, same client outside a transaction, request context, hostile id, 50 concurrent ids, SharedModule-first order; clock 4; mail 4; exports 2); `--detectOpenHandles` clean, and the full suite re-run 3× clean (concurrency test not flaky on this machine); api-driver lint probe 2 errors then 0; bare `@tms/domain` → `ERR_PACKAGE_PATH_NOT_EXPORTED`; hygiene single copy ok (359 tracked files); both app suites 2 suites/6 tests each unchanged; images without the Prisma CLI, `SharedModule` loadable, `/api/health` + `X-Request-Id` echo verified against a throwaway `tms-p1-pg` + `tms-p1-net`, torn down after (images also removed). RED confirmed exactly as predicted: all 4 suites failed to load on `Cannot find module '../../src/shared'`. One deviation from mechanical brief-copying: the brief's own `audit.e2e-spec.ts` snippet does `const minted = res.headers['x-request-id']`, typed `string \| undefined` under this repo's `noUncheckedIndexedAccess: true` (`packages/config/tsconfig/base.json`), which fails `typecheck` (ts-jest's `isolatedModules` doesn't catch it, `tsc --noEmit` does) passing it to `waitForProbeLine(logs, minted)` — fixed with the same `as string` cast already used for the identical situation in `packages/logger/test/logger.e2e-spec.ts:159`, no behavior change. `pnpm-workspace.yaml`/CLAUDE.md/README.md anchors matched the brief's quoted text exactly except CLAUDE.md's `wc -l` count (92 here vs. the brief's expected 79 — the file grew independently in earlier tasks; the bullet content itself was applied as specified). No plan-task-number or spike-label comments found in the new/edited files (checked before committing per the controller ruling); one instance in the brief's own ADR-0006 text ("(spike S1)") was dropped for the same reason. |
| 2026-09-24 | Task 07: db — auth fields migration (`User.lockoutLevel`, `ActionToken @@index([userId, type])`) | inline | 17:20 | 17:45 | none | n/a | First task of Stack B (branched from `main` after phase 1 and Stack A merged). RED confirmed exactly as predicted: `pnpm turbo run test --filter=@tms/db` failed 2/95 (`lockoutLevel` `Expected: 0, Received: undefined`; `ActionToken` index list showed `ActionToken_userId_idx` instead of `_userId_type_idx`; phase 1's other 13 suites stayed green), `pnpm turbo run typecheck --filter=@tms/db` gave TS2339 on `lockoutLevel`. Schema diff and generated `migration.sql` (throwaway `tms-p2-pg` on port 56432 per the plan's Execution notes) matched the brief's predicted output verbatim: `DROP INDEX "ActionToken_userId_idx"`, `ALTER TABLE "User" ADD COLUMN "lockoutLevel" INTEGER NOT NULL DEFAULT 0`, `CREATE INDEX "ActionToken_userId_type_idx" ON "ActionToken"("userId", "type")` — no data-destructive DDL, no table rewrite beyond the column default. GREEN: 95/95 Jest tests (the 2 new plus phase 1's 93 unchanged); `db:migrate:deploy` + `db:drift` against the throwaway database both clean (`Database schema is up to date!`, `No difference detected.`). `pnpm exec prettier --write` on the new spec file reported it already formatted; `.prisma`/`.sql` have no Prettier parser registered in this repo (errors only when named explicitly, `format:check .` does not touch them), consistent with phase 1's `schema.prisma` never being reformatted by Prettier. Touched only the three named files. Throwaway `tms-p2-pg` container and `tms-p2-net` network torn down after the drift check. |
| 2026-09-24 | Task 08: contracts — error envelope, route-access keys, auth DTOs, permissions, audit actions | inline | 22:20 | 22:45 | none | n/a | RED confirmed exactly as predicted: `pnpm turbo run test --filter=@tms/contracts` failed 20/185 across 5 suites (`errors.test.ts`/`auth-schemas.test.ts` unresolved imports; `permissions.test.ts` 33/32 instead of 35/34 plus the new phase-2 test on missing `users:deactivate`/`users:assign-role`; `audit.test.ts` length 50 instead of 55 plus the four new phase-2 cases; `scrub.test.ts` the three `otpauth*` keys not yet sensitive); the other two suites (`roles`, `enums`) stayed green. GREEN on the first implementation pass, no fix round: 7 suites/185 tests, `lint` and `typecheck` both clean. DB prep ordering: implementation (Step 3) finished before the throwaway database was started, and `@tms/db`'s build depends on `@tms/contracts`'s build, so building `@tms/db` in this worktree at that point would already carry the new 35-code catalogue into `dev-setup`, making the sync-permissions diff empty instead of showing the two inserted codes; used a detached temporary `git worktree` at commit `3647f85` (pre-Task-08, the merged Task 07 tip) against the throwaway `tms-p2-pg` on port 56432 to run `dev-setup.js` with the old 33-code catalogue and seeded roles, removed that worktree, then rebuilt `@tms/db` from this branch (cache hit, still the 35-code build) and ran `sync-permissions.js`. Output matched Step 5's exact predicted JSON line: `{"event":"permissions.synced","inserted":["users:deactivate","users:assign-role"],"regrouped":[],"reactivated":[],"renamed":[],"lockedRoleGrants":[{"roleKey":"admin","added":["users:assign-role","users:deactivate"],"removed":[]}],"deprecated":[],"deleted":[]}`; a second run returned all-empty lists (idempotent). `pnpm exec prettier --write` on every created/edited file reported all unchanged (the write/edit hooks had already reformatted on save). Touched exactly the brief's Files list (`git status --porcelain` diffed against it). Throwaway `tms-p2-pg` container and `tms-p2-net` network torn down after Step 5. |
| 2026-09-24 | Task 09: nest-bootstrap — `ApiExceptionFilter`, `ZodValidationPipe`/`zodDto`, cookie-parser, `trust proxy`, helmet | inline | 22:50 | 23:12 | one controller ruling (health controller), one itemization gap found and fixed (`sentry/disabled.e2e-spec.ts`), one lint fix round | n/a | RED confirmed exactly as predicted: 3 new suites failed to load on `Cannot find module '../src/http'`, phase 1's other 13 suites stayed green (117 tests). Plan gap found before finishing Step 3: the brief's literal `ApiExceptionFilter` collapses every `HttpException` with status ≥500 into generic `INTERNAL`/500, which broke `test/health.e2e-spec.ts` (4 tests, not in the brief's Files list, whose Step 4 explicitly predicted it stays green) because `HealthController.check()` deliberately throws `ServiceUnavailableException({status:'degraded',service})` (D5, deviation 9). Paused and reported NEEDS_CONTEXT rather than guess between deviating from the filter's literal code or accepting the regression. Controller ruling: keep the filter exactly as specified (a generic safety net, no per-route special-casing) and instead change `health.controller.ts` to set the 503 response status directly via `@Res({ passthrough: true })` and return the body, bypassing the filter for that one route — byte-identical response in both branches, `health.e2e-spec.ts` needed zero changes; its sibling unit test `health.controller.spec.ts` (calls `check()` directly, not in the brief's Files list either) needed updating for the new signature, a direct and necessary consequence of the ruling. Also found, while re-running the suite after the ruling: `test/sentry/disabled.e2e-spec.ts` asserts the same bare-Nest 500 body as `sentry/capture.e2e-spec.ts` (both built on `createBoomApp()`) but was left out of the brief's itemized edits; applied the identical, already-authorized envelope-body fix rather than asking again. `zodDto`'s `strictObject`/`object` schemas produced two lint-only deviations from the brief's literal test code (not behavior): `INestApplication` typed as `INestApplication<App>` (matching every other e2e spec's convention, avoids `@typescript-eslint/no-unsafe-argument` on `request(app.getHttpServer())`) and three `res.body.<field>` accesses cast through `ApiError`/a small local interface instead of implicit `any` (`@typescript-eslint/no-unsafe-member-access`/`-call`). GREEN: `@tms/nest-bootstrap` 16/16 suites, 138/138 tests (new: filter 10, pipe 4, proxy/headers 7); `@tms/api-admin` and `@tms/api-driver` 2/2 suites, 8/8 tests each with the new envelope 404 body and no `X-Powered-By`. Full `pnpm verify`: 49/49 turbo tasks (lint, typecheck, test, build), repo-wide `prettier --check .` clean, `pnpm hygiene` ok (385 tracked files, single copies ok), gitleaks no leaks. Touched exactly the brief's Files list plus the two controller-authorized additions (`health.controller.ts`/`.spec.ts`) and the one itemization gap (`sentry/disabled.e2e-spec.ts`); Playwright smoke (`e2e/tests/support/smoke.ts`) edited per the brief but deferred to CI's e2e job, not run locally, matching Step 4's own note. |
| 2026-09-25 | Task 10: Route access markers, `AccessGuard` and `OriginGuard` | inline | 07:05 | 07:40 | two lint-only fix rounds (see notes), one deliberate scope extension beyond the brief's literal instruction (see notes) | n/a | RED confirmed exactly as predicted: `pnpm turbo run test --filter=@tms/nest-bootstrap` failed 1/17 suites (`OriginGuard is not a constructor`); `pnpm turbo run test --filter=@tms/domain` failed 3/6 suites (`exports.spec.ts` 1/2 wrong list, `route-scan.spec.ts`/`access.guard.spec.ts` failed to load on `Public is not a function`); phase 1's other suites in both packages stayed green. GREEN after Steps 3–5: `@tms/nest-bootstrap` 17/17 suites (156 tests, +16 Origin matrix); `@tms/domain` 6/6 suites (30 tests, +10: access.guard 9, route-scan 1); `@tms/api-admin` 3/3 suites (11 tests: env 1, route-access 3, app.e2e-spec 7); `@tms/api-driver` 3/3 suites (9 tests: env 1, route-access 1, app.e2e-spec 7) — all green on the first run against the real apps, no fix round needed for the app wiring itself. Two lint-only fix rounds followed (behavior unchanged): (1) the brief's own literal test snippets tripped `@typescript-eslint/no-unsafe-*` under this repo's `recommendedTypeChecked` preset — `FakeResolver.resolve()`'s `async` with no `await` (`require-await`), untyped `INestApplication`/`request(app.getHttpServer())` against supertest's `App` generic, and `.body.code` access on `any` — fixed with the same pattern already established in Task 09's journal (`INestApplication<App>` + `createNestApplication<INestApplication<App>>(...)`, a typed `codeOf`/`rejectsOrigin` helper per file) in `origin.guard.spec.ts`, `access.guard.spec.ts` and `route-access.e2e-spec.ts`; (2) `route-scan.ts`'s `const { instance, metatype } = wrapper` destructured `InstanceWrapper<T = any>.instance` (any), fixed with `wrapper.instance as object | undefined` (a bare type annotation did not satisfy the rule, only a cast did). `apps/api-admin/test/route-access.e2e-spec.ts` originally imported `ApiError` from `@tms/contracts` to type `.body.code`, but api-admin's `package.json` has no direct dependency on `@tms/contracts` (not in the brief's Files list to modify) — reverted to a local `{ code: string }` cast instead of adding an undeclared dependency. Deliberate scope extension: the brief's Step 5 says only "the test-only `ProbeController.get` gains `@Public()`", but both apps' `test/app.e2e-spec.ts` also register an `AuditProbeController` on the same real `AppModule` (`GET /api/probe/audit`), which the new fail-closed `AccessGuard` would otherwise reject with `ROUTE_NOT_DECLARED` and break the existing "agrees on the request id..." test that Step 7 requires to stay green; added `@Public()` to `AuditProbeController.auditProbe` in both apps for the same reason, not only `ProbeController.get`. Full `pnpm verify`: 49/49 turbo tasks (lint, typecheck, test, build) green, repo-wide `prettier --check .` clean after one `--write` on the new ADR (a markdown reflow, no content change), `pnpm hygiene` ok (392 tracked files, single copies ok — the `@tms/config` boundaries suite's existing `domain -> auth-core` allowance covered the new dependency edge, no boundaries change needed), gitleaks no leaks. CLAUDE.md 93 lines (limit 150). Touched exactly the brief's Files list. |
| 2026-09-25 | Task 11: Sessions — `SessionService`, `StaffSessionResolver`, cookie, logout, session state | inline | 07:45 | 08:36 | none beyond the deviations noted (all pre-commit, first implementation pass green) | n/a | RED confirmed exactly as predicted: `pnpm turbo run test --filter=@tms/domain` failed 2/8 suites (`evaluate-session.spec.ts`/`session-cookie.spec.ts` on `Cannot find module '../../src/admin/...'`, phase 1's 6 suites stayed green); `@tms/api-admin` failed 4/5 suites (`env.spec.ts` missing the three session defaults and the production case, `session.e2e-spec.ts`/`prisma-errors.e2e-spec.ts`/`route-access.e2e-spec.ts` on the missing `@tms/domain/admin` module and the missing snapshot rows); `@tms/api-driver` failed 1/3 (404 case missing `no-store`). GREEN on the first implementation pass: `@tms/domain` 8/8 suites (37 tests, +6: evaluate-session 3, session-cookie 3; the pre-existing `exports.spec.ts` case was updated in place, not added); `@tms/api-admin` 5/5 suites (26 tests, +15 over Task 10's 11: env +1, `session.e2e-spec` new at 12 incl. `it.each`, `prisma-errors.e2e-spec` new at 2); `@tms/api-driver` 3/3 suites (9 tests, +1 no-store case, no new suite). Three deviations from mechanical brief-copying, all required for real compilation, none behavioral: (1) `tsc -p tsconfig.build.json` (declaration emit) rejected the brief's literal untyped `findByToken` with TS2883 ("inferred type ... cannot be named without a reference to ... not portable") on the Prisma `Session`+`user`+`role.permissions` include shape — fixed by naming the query args once as `SESSION_WITH_USER` (`satisfies Prisma.SessionDefaultArgs`) and giving `findByToken` an explicit `Promise<SessionWithUser | null>` return type via `Prisma.SessionGetPayload<typeof SESSION_WITH_USER>`, importing `type { Prisma } from '@tms/db'`; (2) `apps/api-admin/package.json` had no `@tms/contracts` dependency (same gap Task 10's journal noted and worked around) but this task's brief code imports `SessionStateResponse` in `session.controller.ts` and `SessionScope`/`UserKind`/`UserStatus` in `test/support/fixtures.ts` directly from `@tms/contracts` — added it to `dependencies` (matching how `@tms/nest-bootstrap` and `@tms/domain` already declare it), `pnpm install` re-run; (3) `packages/domain/package.json` had no `fast-check` devDependency, needed by the brief's own `evaluate-session.spec.ts` — added under `catalog:`, `pnpm install` re-run. Also: the task description's claim that `apps/api-admin/src/env.ts` already carried a refinement chained after `.extend()` did not match `main`'s actual file (no refinement existed yet); added the object literal's three new keys and a fresh `.refine(...)` directly after `.extend(...)` per the brief's own Step 6 instructions, which are self-consistent without a pre-existing refinement. One lint-only fix round in `session.e2e-spec.ts`: `.body.code` access on `any` (2 places), fixed with the same typed `codeOf` helper pattern Task 10 established. Full `pnpm verify`: 49/49 turbo tasks (lint, typecheck, test, build) green, repo-wide `prettier --check .` clean after one `--write` on `route-access.snapshot.json` (reflow only), `pnpm hygiene` ok (409 tracked files, single copies ok), gitleaks no leaks. Touched the brief's Files list plus `apps/api-admin/package.json` (new `@tms/contracts` dependency) and `packages/domain/package.json` (new `fast-check` devDependency), both undeclared-dependency fixes required for the brief's own literal code to compile. |

## Decisions for Stefan

- Repository LICENSE: the repository is public and has none, which means all rights reserved; pick a license or confirm that is intended.
- Whether `e2e` stays a required check on `main` once its duration is known (~2 min in CI).
- Delete the now-unused local `docs/client/forbidden-terms.txt` (ADR 0007 removed the check that read it) and confirm the tracked `docs/client/README.md` (main's version, kept by the split) reads as intended.
- Add the required status checks (`verify`, `hygiene`, `db-drift`, and `e2e` per the item above) to the `main` ruleset after the stack merges.
- pnpm's release-age gate is advisory only today (no `minimumReleaseAge` set beyond the auto-generated `minimumReleaseAgeExclude`); recommend setting it strict at `minimumReleaseAge: 1440` (24h) plus a matching Dependabot cooldown, so a freshly published (and potentially compromised) package version cannot land before the community has had a day to flag it (architect review, workspace group, W6).
- Connect the Claude in Chrome extension, or accept that visual frontend verification stays `not run`: Task 12 Step 1 found the extension not connected (nested `claude --chrome -p` loaded the tools but reported it disconnected), and spec §13 step 4 calls for a `claude --chrome` pass with screenshots on every frontend PR.

## Inputs for later phase plans

One line per item: what, why, and the phase that owns it. Sourced from the final whole-branch
review and the architect reviews of the published PR stack.

- ~~Phase 1 (task 1, before any cross-package import lands): `eslint-plugin-boundaries` never fires
  for `@tms/*` specifiers (only relative imports resolve) — add
  `eslint-import-resolver-typescript` and a specifier-based test; phase 0 has no package pair the
  rule could protect yet, but phase 1 is the first to add one (architect review, workspace group,
  W1).~~ — done: `eslint-import-resolver-typescript` wired into `packages/config/eslint/base.mjs`
  (W1).
- ~~Phase 1: decide where the shared Nest bootstrap lives before a third copy of it appears —
  `@tms/logger` or a small bootstrap package (final review).~~ — done: `packages/nest-bootstrap`
  (`@tms/nest-bootstrap`).
- ~~Phase 1: a `node-library` tsconfig preset vs the package-local `"types": ["node"]` added to
  `packages/db/tsconfig.json` — decide before `auth-core`/`domain`/`logger` each repeat it (final
  review).~~ — done: `packages/config/tsconfig/node-library.json`.
- ~~Phase 1: the `migrate` image is 760MB — switch to `deploy --prod`, add
  `COPY --chown=node:node` (currently root-owned under `USER node`), and reconsider the
  `@prisma/dev` footprint it pulls in (final review).~~ — done, with two of the three sub-items
  decided differently than proposed: Task 09 switched the build to `pnpm deploy --legacy --prod`;
  `/app` stayed root-owned under `USER node` on purpose (deviation 10 — the runtime user must not
  be able to rewrite its own code) instead of `COPY --chown`; the `@prisma/dev` footprint was
  measured (`/app` 336MB) and accepted inside a new CI size budget (`MIGRATE_APP_MAX_BYTES`) rather
  than pruned further.
- ~~Phase 1: `predev` switches from `prisma migrate deploy` to `migrate dev` + permission sync, per
  spec D12 (final review; also the subject of ADR-0002's D10-8 fix).~~ — done, though the verb kept
  is `migrate deploy`, not `migrate dev`: `predev`/`db:setup`
  (`packages/db/src/cli/dev-setup.ts`) runs `migrate deploy`, a drift check, the permission sync and
  the seed, in that order.
- ~~Phase 1: turbo's `test` task prints "no output files found" noise because test tasks declare
  `outputs: ["coverage/**"]` with no coverage configured yet — set `outputs: []` (architect review,
  docs group, D10-3).~~ — done: `turbo.json`'s `test` and `@tms/db#test` tasks both set
  `outputs: []`.
- ~~Phase 1: narrow PR #3's body claim "Review Focus 4 is tested" to what is actually covered, and add
  a process-level `PORT` env test (architect review, apps group, A-4).~~ — done:
  `apps/api-admin/test/env.spec.ts` and `apps/api-driver/test/env.spec.ts` cover `PORT`.
- ~~Phase 1: `HOST` env default — bind `127.0.0.1` in dev vs `0.0.0.0` in compose — is undecided
  (architect review, apps group, A-6).~~ — done: `listenHost()` in
  `packages/nest-bootstrap/src/env.ts` (127.0.0.1 in development, 0.0.0.0 otherwise, or `HOST`).
- ~~Phase 2 (re-tagged from phase 1 — the journal's own Task 12a/b entry already deferred this
  alongside A-7): a cross-origin negative check once api-admin and api-driver actually diverge
  enough for one to matter (architect review, apps group, A-8).~~ — done in phase 2/10 (A-8): the
  `OriginGuard` matrix and the api-admin route-access e2e cases.
- ~~Phase 1: land the kiosk `/api` → api-driver routing-identity check (today's smoke/e2e tests can't
  tell api-admin and api-driver apart by their identical 404s) once D5's `/health` exists and
  returns the service name; a stop-api-admin smoke step would make smoke stateful, so this waits for
  `/health` instead (architect review, infra group, I7-1).~~ — done: `/api/health` returns
  `{status,service}` and `infra/smoke.sh`'s `check_origin` asserts the service name per API (I7-1).
- ~~Phase 1: API/Caddy healthchecks, restart policies and a migrate-ordering proof, together with
  `/health` (architect review, infra group, M6-6).~~ — done: `infra/docker-compose.yml` has
  healthchecks on both APIs, `restart: unless-stopped`, and `depends_on: … condition:
  service_completed_successfully` ordering the APIs after `migrate` (M6-6).
- ~~Phase 1/4: split the single `app` boundaries element type into separate api/web element types once
  `packages/domain` lands (architect review, workspace group, W5).~~ — done:
  `packages/config/eslint/base.mjs` already has separate `api`/`web` boundaries element types (W5).
- ~~Phase 2: remove the `X-Powered-By` header via helmet~~ — done in phase 2/09 (A-7).
- Phase 2: give `auth-core` its own `no-restricted-imports` rule against `@nestjs/*`/`@prisma/*`
  specifiers when the package lands — `eslint-plugin-boundaries` only stops it importing
  `db`/`domain`, it does not check npm-package specifiers (architect review, docs group, D10-6).
- ~~Phase 3a (prep): hooks resolve the project root per edited file~~ — done in phase 3a/02 (9-M1).
- Phase 4/5: `no-restricted-imports` re-includes only the literal `@tms/domain/checkin` and
  `@tms/domain/shared` subpaths (matches spec section 6's two subpath exports exactly) — revisit
  once `packages/domain` exists and could export more (phase 0 Task 1 minor, restated by architect
  review docs group D10-3).
- Phase 5: an api-driver subpath-import test alongside the app-boundary tests, done together with
  W1/W5 once both land (architect review, apps group, A-9).
- Phase 6: regenerate the vendored MSW worker (`apps/*/public/mockServiceWorker.js`) when `msw` is
  bumped (final review).
- Phase 6: an automated check for the Vite dev-proxy behaviour (today only manually verified)
  (architect review, apps group, W-1b).
- Phase 6: guard the MSW worker so it is dev-only and never ships inside a production build image
  (architect review, apps group, W-4, together with M6-7's prod-image check below).
- Phase 6: page titles and `lang` attribute via i18n keys or an explicit, documented brand exception
  (architect review, apps group, W-6).
- Phase 9: cache gitleaks in CI, SHA-pin third-party actions, add Docker layer caching
  (`pnpm fetch`), run Caddy as non-root (final review).
- Phase 9: a missing hashed static asset falls through to a 200 `index.html` instead of a 404
  (architect review, infra group, M6-7).
- Phase 9: give `e2e/tests/support/smoke.ts`'s looped API assertions a per-path `test.step` so a
  failure names the path instead of only the project (phase 0 Task 6 minor, restated by architect
  review docs group D10-3).
- Phase 9: de-duplicate ports, commands and the dependency rule, which are currently repeated
  verbatim across `CLAUDE.md`, `README.md` and `docs/architecture.md`, with the `docs-sync` skill
  (architect review, docs group, D10-3).
- Phase 10: plugin `format.mjs` FORMATTABLE/SKIP_DIRS case-insensitivity, and temp-dir cleanup in
  `hooks-cli.test.mjs` (final review).
- Phase 10: write `docs/RETROSPECTIVE.md`, which `README.md` already cites as an "end of POC"
  deliverable (architect review, docs group, D10-3).
- Unphased: confirm that Dependabot's `npm` ecosystem actually bumps the pnpm `catalog:` entries in
  `pnpm-workspace.yaml` on its first weekly run — declined to judge without a live run (architect
  review, workspace group, D10-3).
