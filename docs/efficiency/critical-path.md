# Efficiency journal: critical-path lane

One entry per task. Fill in as you go, not afterwards. Times are local (Europe/Belgrade).
Approach values: `inline` (Claude in main session), `subagent` (dispatched agent), `skill`
(named skill), `parallel-session` (separate worktree + session), `human`.

| Date | Task | Approach | Start | End | Rework | Critic findings (total / accepted / prevented rework) | Notes |
|---|---|---|---|---|---|---|---|
| 2026-09-23 | Analyse 4 client documents (123-page functional description, architecture drawing, roadmap, proposal deck) | inline (pdftotext + targeted reads) | 08:15 | 08:40 | none | n/a | Extracting text to scratchpad then reading only relevant sections kept context small. |
| 2026-09-23 | Requirements interview (brainstorming skill, 3 rounds of grouped questions + 3 design rounds) | skill: superpowers:brainstorming | 08:40 | 09:35 | none | n/a | Grouping 2-4 related questions per round worked well; user added requirements mid-turn twice (PR verification, parallelisation). |
| 2026-09-23 | Verify Claude Code facts (chrome flag, plugin layout, hooks, best-practices doc) | subagent: claude-code-guide | 09:00 | 09:01 | none | n/a | Ran in background during interview; avoided guessing. |
| 2026-09-23 | Critic pass over spec (Disney "critic") | subagent: Plan (read-only, fresh context) | 09:40 | 09:46 | n/a | 25 / 23 full + 2 partial / est. 7 HIGH would have caused rework (TOTP brute force, admin without MFA, sequence race, unique orderId, tx boundaries, phase order, scope) | Strong signal the critic pass pays for itself at spec level. |
| 2026-09-23 | Incorporate critic + user review comments, finalise plan | inline | 09:46 | 10:05 | plan rewritten once (full rewrite cheaper than 25 edits) | n/a | Plan approved 10:05. |
| 2026-09-23 | Repo hygiene (move client docs, .gitignore, git init), spec written to repo, memory saved | inline | 10:06 | 10:10 | none | n/a | Spec written in English with the client anonymised because the repo is public. |
| 2026-09-23 | Create public GitHub repo, squash-only merges, ruleset on main (PR required, no force push/deletion, linear history) | inline (gh CLI) | 10:14 | 10:18 | none | n/a | Required status checks to be added to the ruleset once CI workflow job names exist (phase 0). |
| 2026-09-23 | Phase 0 implementation plan (13 tasks, `docs/superpowers/plans/2026-09-23-phase-0-bootstrap.md`) | skill: superpowers:writing-plans (+ claude-code-guide subagent for plugin/hook formats) | 10:20 | 11:05 | self-review fixed 5 issues before the critic (TDD order, e2e `test` script, Prisma env in CI, nested fences, tsconfig includes) | n/a (critic pass follows) | Fact-finding before writing (npm view, official Nest 12 / Vite 8 / Prisma 7 templates in scratch, ESLint 10 config lookup test, gitleaks checksum flow) corrected 6 assumptions that would have broken tasks: TS 7 unsupported by Nest CLI and typescript-eslint, Prisma `latest` is an 8 rc, pnpm 12 `allowBuilds`, Nest 12 ESM + Jest `--experimental-vm-modules`. Two "facts" from the guide subagent (no non-blocking Stop output, directory marketplace undocumented) were wrong and caught by the critic. |
| 2026-09-23 | Critic pass over the phase 0 plan (Disney "critic") | subagent: Plan (read-only, fresh context) | 11:05 | 11:12 | n/a | 24 / 24 / est. 7 (3 HIGH: eager Prisma `env()` breaking CI, no guard for the client name, missing root eslint config; plus 4 MEDIUM that would have failed a task's own acceptance step) | The critic verified facts read-only (tarballs, `npm view`, docs) and overturned two claims the guide subagent had made. Second consecutive critic pass that paid for itself; keep it mandatory per phase. |
| 2026-09-23 | Incorporate the 24 critic findings into the plan | inline | 11:12 | 11:45 | Tasks 5 and 10 split (5a/5b, 10a/10b); 3 facts corrected | n/a | Targeted edits (about 40) were cheaper than a rewrite this time because findings were local; the spec-level pass needed a rewrite. |
| 2026-09-23 | Task 1: workspace root + `@tms/config` (tsconfig/eslint/jest/vitest presets) | subagent (sonnet) | 11:44 | 11:58 | Two brief snippets did not pass their own acceptance tests as written and needed a minimal fix: `no-restricted-imports` used one glob `group` with a bare `@tms/domain` entry plus negated subpaths — gitignore-style matching (the `ignore` package) cannot re-include a child once a parent is excluded, so the bare specifier had to move to a separate `paths` ban, decoupled from the `patterns` group; and `eslint-plugin-boundaries` v7 could not resolve extensionless relative `.ts` imports at all (silently reported no dependency) until `settings['import/resolver'].node.extensions` included `.ts`/`.tsx` — its default resolver (`eslint-import-resolver-node`) only tries `.js/.json/.node`. Also added a package-local ignore for `test/fixtures/**` in `packages/config/eslint.config.mjs` so `eslint .` does not try to type-check the boundaries fixtures (they are fed to ESLint in-memory by the test, not part of this package's TS project). No extra `allowBuilds` entries were needed; the brief's list (esbuild, prisma, @prisma/engines, msw) was sufficient — `pnpm install` produced no `ERR_PNPM_IGNORED_BUILDS`. `pnpm install` also auto-added a `minimumReleaseAgeExclude` block to `pnpm-workspace.yaml` for freshly-published catalog packages (pnpm 12 default minimum-release-age gate); left as pnpm produced it. |
| 2026-09-23 | Task 2: NestJS API skeletons (`api-admin`, `api-driver`) | subagent (sonnet) | 12:07 | 12:13 | Primary Jest config (ts-jest, no `useESM`) worked unmodified, so the documented ESM fallback was not needed. The brief's own e2e-spec assignment (`configureApp(moduleRef.createNestApplication())`) failed `no-unsafe-assignment` because `configureApp`'s fixed signature returns untemplated `INestApplication` (`= any`); fixed with an `as INestApplication<App>` cast, no behavior change. `pnpm --filter a --filter b lint typecheck` does not run two scripts (pnpm passes `typecheck` as an arg to `lint`), so lint and typecheck were run as two separate commands. | n/a | `ExperimentalWarning` appeared only once the e2e-spec (not the plain unit spec) loaded; silenced via `NODE_OPTIONS='--experimental-vm-modules --no-warnings=ExperimentalWarning'` per the brief's allowance. Also had to set `@parcel/watcher`/`unrs-resolver` to `true` in `allowBuilds` (pnpm auto-added them as unresolved placeholders on `pnpm install`). |
| 2026-09-23 | Task 3: Vite SPA skeletons with MSW (`web-admin`, `web-driver`) | subagent (sonnet) | 12:22 | 12:32 | Brief listed the render test only under `web-admin`, but Step 4 expects "2 passed per app", so `App.test.tsx` was mirrored to `web-driver` verbatim (implied by "mirrors everything"). `web-driver`'s `package.json` description was given a driver-specific value ("Kiosk SPA (driver check-in)...") for consistency with `api-driver`'s own deviation in Task 2, since the brief only spelled out the admin string. | n/a | `pnpm install` needed no new `allowBuilds` entries. `msw init public --save` left `package.json`'s existing `msw.workerDirectory` field untouched (already matched); Prettier reflowed it to multi-line JSON, which is expected repo style. Dev-proxy check against the Task 2 API confirmed a JSON `{"statusCode":404,...}` body through the Vite proxy, and both `timeout`-wrapped background processes exited on their own. |
| 2026-09-23 | Task 4: Prisma `db` package skeleton (`packages/db`, no models) | subagent (sonnet) | 12:31 | 12:38 | `library.json` (unlike `nest.json`/`react-node.json`) sets no explicit `types`, so `@types/node` was never auto-picked up for `prisma.config.ts`'s `process.env` access, failing both `typecheck` (TS2591) and `lint` (`no-unsafe-assignment` on the error-typed value); fixed with a package-local `"types": ["node"]` in `packages/db/tsconfig.json`, matching the pattern already used by the other two presets. | n/a | Verified both without a database (`db:migrate:deploy` failed with Prisma's missing-datasource error, exit 1, as required) and against a throwaway `postgres:16-alpine` container (`migrate deploy` and `db:drift` both exit 0 on the empty schema). The "Update available 7.10.0 -> 8.0.0-rc" banner appeared once and was intermittent (Prisma's own update-check cache); `CHECKPOINT_DISABLE=1` is Prisma's documented env var to silence it but was not added since the brief's scripts don't call for it. |
| 2026-09-23 | Task 5a: docker-compose default profile (postgres, mailpit, migrate) and smoke test | subagent (sonnet) | 12:40 | 12:52 | The `migrate.Dockerfile` as given built and passed `compose config`, but the container exited 1 with Prisma's "please make sure you install prisma with the right permissions": the build stage lacked `openssl`, so the engine postinstall defaulted to `openssl-1.1.x`, then at runtime (openssl 3.0 installed, `USER node`, root-owned copied files) Prisma tried to re-fetch/write the correct engine and had no permission; fixed by installing `openssl`/`ca-certificates` in the build stage too, matching the runtime stage, so the correct engine is fetched at build time. | n/a | Verified end to end: `compose config -q` clean, `up -d --build` (~53s cold, ~8s cached), `smoke.sh` all green, negative check (wrong password) exited 1 with Prisma's P1000 auth error as required, then restored the stack. `pnpm --filter @tms/db deploy --legacy` worked as written, no pnpm-workspace.yaml change needed. Final `migrate` image: 760MB. |
| 2026-09-23 | Task 5b: docker-compose `full` profile — API images, SPA images behind Caddy | subagent (sonnet) | 12:53 | 13:05 | Initial round: none — `api.Dockerfile`, `web.Dockerfile` and `Caddyfile` built and ran unmodified from the brief; `pnpm --filter X deploy --legacy --prod /out` was accepted by pnpm 12.5.1 as given, so the `injectWorkspacePackages` fallback was not needed; the API skeletons have no Prisma dependency, so the Task 5a `openssl` lesson did not apply. Fix round 1 (14:40–14:55, task review, Important): the brief's `handle /api/*` matcher does not match the bare `/api` path (no trailing slash) — Caddy fell through to the SPA handler and served `index.html` instead of the API's JSON 404, violating D11. Fixed by widening the matcher; `handle /api /api/*` (the brief's shorthand form) is rejected by Caddy at startup ("wrong argument count... after '/api/*'" — the `handle` shorthand only accepts one path token), so used a named matcher instead: `@api path /api /api/*` + `handle @api { ... }` on both site blocks. Added a regression check to `infra/smoke.sh --full` (bare `/api` on both origins must return 404 + `application/json`). | n/a | Verified end to end: `compose config -q` (with `--profile full`) clean, `up -d --build` (~77s cold), `infra/smoke.sh --full` all green (both origins serve their SPA title and forward `/api/does-not-exist` to a JSON 404). Additional deep-link check beyond the smoke script: `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/some/route` and the 8081 kiosk equivalent both returned `200` with the SPA's `index.html` (via Caddy `try_files`), confirming client-side routing survives a hard refresh. Final image sizes: `tms-api-admin` 364MB, `tms-api-driver` 364MB, `tms-caddy` 89.1MB. Fix round 1 re-verified: `smoke.sh --full` green including the new bare-`/api` checks, and `curl -D- http://localhost:8080/api` / `:8081/api` both show `HTTP/1.1 404 Not Found` + `Content-Type: application/json; charset=utf-8`. Full stack left running for Task 6. |
| 2026-09-23 | Task 6: Playwright skeleton | subagent (haiku) | 14:15 | 14:35 | Fix round 1: Review found duplicated test logic. Created `e2e/tests/support/smoke.ts` with `registerSmokeSuite({ name, title, api })` helper that registers three parameterized smoke tests per project. Updated each project's `smoke.spec.ts` to just call the helper. API test now loops over both `/api` and `/api/does-not-exist` paths in one test. Fixed ESLint import resolution by adding `.js` extension. | n/a | Created e2e workspace with Playwright projects for web-admin and web-driver. All 6 smoke tests (3 per project) passed: SPA serves on correct origin, API forwards with JSON 404 for both paths, deep links work. Lint and typecheck clean. pnpm install auto-added no workspace.yaml entries. Stack remains running. |
| 2026-09-23 | Task 7: `@tms/scripts` hygiene check and pinned, checksum-verified gitleaks wrapper | subagent (sonnet) | 13:05 | 13:40 | TDD RED confirmed (`check-hygiene.mjs` missing, import error) before GREEN (13/13 vitest). The brief's own `.mjs`-only package did not hit the anticipated type-aware-parser gap (`disableTypeChecked` already covers `**/*.{js,mjs,cjs}` in the shared base config), so no package-local tsconfig fix was needed, contrary to the pre-flagged risk. Authoring slip while seeding the local, gitignored `forbidden-terms.txt`: its own comment prose used commas, and `parseForbiddenTerms` (per its own tested spec) splits on commas too, so comment fragments after a comma leaked through as bogus search terms and matched unrelated prose repo-wide; caught immediately by the brief's own clean-tree exit-0 check, fixed by rewriting the comment without commas — no script change. Fix round 1 (task review Important finding + controller ruling on my own comma concern): (1) `gitGrepForbidden` rethrew the raw `execFileSync` error on any git-grep failure other than exit 1 (e.g. fatal "not a git repository", exit 128), and that error's `.message` embeds every `-e <term>` argument verbatim; fixed to throw a sanitized `Error('git grep failed (exit N)')` with no `cause`. The new core ESLint rule `preserve-caught-error` then demanded a `cause` on that rethrow, directly conflicting with the fix's intent; resolved with a scoped `eslint-disable-next-line` plus a comment explaining why attaching `cause` would defeat it, rather than changing `packages/config`. (2) `parseForbiddenTerms` split on commas before dropping `#` lines, so a comment line containing a comma could still leak fragments (this was the root cause of my own earlier authoring slip); fixed to strip whole comment lines first, then split the remainder. Two new tests added, RED confirmed (both failed against the pre-fix code, reproducing the leak live) then GREEN: a non-git-repo `gitGrepForbidden` call with a dummy term asserts neither the thrown message nor `.cause` contain it; `parseForbiddenTerms('# names, one per line\nAcme')` → `['Acme']`. | n/a | Seeded the local terms file from `docs/client/` file names only (never opened the documents), excluding generic/product words per the controller ruling; verified each real term individually trips `--staged` with exactly one `file:line` line and no leaked text (4/4), and that comment lines correctly do not trip it. `gitleaks.sh` first run downloaded and checksum-verified 8.30.1 into `.cache/gitleaks/8.30.1/` (network available); repo scan and `gitleaks.test.sh` (3/3 `ok`, offline reuse + tampered-checksum rejection) both green. `pnpm install` added no new `pnpm-workspace.yaml` entries. Fix round 1 re-verified: 15/15 vitest + 3/3 gitleaks + eslint clean + hygiene exit 0 + `pnpm format:check` exit 0. |
| 2026-09-23 | Task 8: Git hooks (husky, lint-staged, commitlint) | subagent (haiku) | 13:18 | 13:24 | none | n/a | Created `commitlint.config.mjs`, `.husky/pre-commit`, `.husky/commit-msg`, `.husky/pre-push` as specified. All commitlint tests passed (bad message rejected, valid message accepted, long header rejected). ESLint passed on commitlint config. Pre-commit hook detected and rejected a seeded GitHub token via gitleaks (exit 1, "leaks found: 1"). Pre-commit hook formatted a demo file correctly and committed it successfully. Both demos cleaned up successfully. Hooks installed and verified via `git config core.hooksPath` = `.husky/_`. |
| 2026-09-23 | Task 10a: Claude Code plugin hooks (protect, format, verify reminder) | subagent (sonnet) | 13:25 | 13:50 | Fix round 1 (task review, Important): `classifyEdit`'s `.env`/`docs/client/` comparisons were case-sensitive, giving a false allow on case-varied paths (`DOCS/Client/x.pdf`, `.ENV`, `apps/api-admin/.ENV.LOCAL`, etc.) — a real risk on case-insensitive filesystems (macOS APFS, Windows/NTFS default) where such a path is the same file on disk as its lower-case form, so Claude could be routed around the hook. Fixed by folding to lower case only for the comparison (`base.toLowerCase()`, `rel.toLowerCase()` for the `docs/client/` prefix, `.env`/`.env.`/`.example` all folded), keeping the original `filePath` untouched in the reason string; `docs/clientele/` stays allowed in any case. Added 6 tests to `protect.test.mjs` (4 new blocked case-varied paths, 2 new allowed look-alikes `.Env.Example`/`DOCS/Clientele/x.pdf`); RED confirmed (4 failed as expected) before GREEN (61/61). | n/a | TDD RED confirmed (3 test files failed to import `lib/{protect,format,reminder}.mjs`; the 11 CLI tests failed because the hook scripts did not exist yet) before GREEN (55/55 vitest: protect 20, format 20, reminder 4, cli 11 — matches the brief's Step 4 breakdown exactly, not the plan overview's 54). All brief snippets worked unmodified, no lint fixes needed. `pnpm install` made no `pnpm-workspace.yaml` changes this run (`tools/*` already covered it). `claude plugin validate tools/claude-plugin/ethernal-nest-react --strict` passed clean (exit 0). Sanity-checked the package's own `validate` script beyond Step 4's ask: its first command (`claude plugin validate . --strict` against `tools/claude-plugin` itself) fails because that directory has no `.claude-plugin/marketplace.json` yet — expected, since Task 10b adds the marketplace manifest there; not a bug, left as specified. Fix round 1 re-verified: 61/61 vitest (protect 26, format 20, reminder 4, cli 11), eslint clean, `pnpm format:check` exit 0, and the package's own `validate` script now passes in full (Task 10b added the marketplace manifest in the meantime). |
| 2026-09-23 | Task 10b: plan-critic agent, verify/pr skills, marketplace, `.claude/settings.json` | subagent (sonnet) | 13:50 | 14:20 | Fix round 1 (task review, Important): `Bash(git branch:*)` was a prefix match that also allowed mutating subcommands (`-D`, `-m/-M`, `-f`) under a read-only-sounding name; narrowed to the exact-match `Bash(git branch --show-current)` in `.claude/settings.json`, no other entries touched | n/a | `CLAUDE_BIN=~/.local/bin/claude pnpm --filter @tms/claude-plugin validate` passed clean, exit 0 (marketplace then plugin, both "Validation passed"), no `$schema`/frontmatter issues so nothing needed to be removed. Load check: `~/.local/bin/claude -p "List the plugins..."` in a fresh nested session reported `ethernal-nest-react@tms` **enabled in `.claude/settings.json` but not loaded** (not present in `installed_plugins.json` — needs interactive trust/installation of the project marketplace, as anticipated by the brief's fallback). Per the brief's resolution, re-ran with `--plugin-dir tools/claude-plugin/ethernal-nest-react`: plugin loaded correctly and named all three hooks (`protect-files` PreToolUse, `format-on-edit` PostToolUse, `verify-reminder` Stop), the `plan-critic` agent, and both `pr`/`verify` skills — proves the plugin itself is sound; left the marketplace `path` relative (not changed to absolute, per instruction) and `pnpm claude` remains the documented interactive-session fallback. Manual hook check, 3 nested `-p` sessions (`--plugin-dir ... --allowedTools Edit`), each restored with `git checkout --` immediately after and confirmed via `git status --short`: (1) append comment to `apps/api-admin/.env.example` — **allowed**, edit applied (verified `grep`), nested session's own Stop hook additionally fired a `pnpm verify` reminder and it ran clean; (2) append line to `docs/client/README.md` — **blocked**, exact reason returned: "File is in a directory that is denied by your permission settings" (the settings.json `Edit(/docs/client/**)` deny rule fired ahead of the plugin's own hook, confirming the brief's "always-on layer" design; file untouched, `git status --short docs/client/` empty); (3) bad spacing added to `apps/api-driver/src/app.ts` (`demoHookCheck   =    1`) — **formatted after edit**, verified via `grep`: `export const demoHookCheck = 1;` (single spaces). All three restored; working tree clean apart from this task's new files at each check. |
| 2026-09-23 | Task 11: Documentation (CLAUDE.md, README, PR template, architecture, ADRs) | subagent (haiku) | 14:36 | 14:50 | Fix round 1 (task review, Important): Task entry misreported CLAUDE.md line count as 130; corrected to 61 (confirmed by `wc -l CLAUDE.md` output in Step 5 and commit verification). Also noted that CI job names were taken from the phase 0 plan, not verified against .github/workflows/ (Task 9 has not been written yet). | n/a | Facts verified against the real repository (root scripts, docker-compose profiles, plugin location) — all matched the brief exactly, no deviations. Created 8 documentation files: CLAUDE.md (61 lines, within 150-line limit), README.md, .github/pull_request_template.md, docs/architecture.md, docs/adr/{README.md,0000-template.md,0001-two-nest-processes-shared-packages.md,0002-prisma-orm.md}. All files use the exact text from the brief without modification. Created .github directory. Verified Step 5 checks (`node tools/scripts/check-hygiene.mjs && pnpm format:check && wc -l CLAUDE.md`), formatted all files, and verified commit preconditions. |
| 2026-09-23 | Task 9a: client name by convention (decision C), forbidden-terms check removed, ADR 0007 | subagent | 15:05 | 15:25 | none | n/a | Removed `FORBIDDEN_TERMS_FILE`, `parseForbiddenTerms`, `gitGrepForbidden`, the `forbiddenHits` input of `runHygiene` (now `runHygiene({ trackedFiles, claudeMd })`) and the `--staged` flag from `tools/scripts/check-hygiene.mjs`; the client-type document check and CLAUDE.md length check are unchanged. `.husky/pre-commit` no longer passes `--staged` to the hygiene script (gitleaks keeps its own `--staged`, a different flag). `check-hygiene.test.mjs` dropped the `parseForbiddenTerms`/`gitGrepForbidden` describe blocks, the "reports forbidden-term hits by location only" test and the now-unused `tempRepo()` helper: 15 tests before, 7 after (all green), plus 3/3 gitleaks tests unchanged. Documented the decision in `docs/adr/0007-client-name-by-convention.md` (option C, table of A/B/C reproduced) and cross-referenced it from `CLAUDE.md`, `README.md` (dropped the `forbidden-terms.txt` prerequisite bullet) and `docs/adr/README.md`. `node tools/scripts/check-hygiene.mjs` now prints `hygiene: ok (157 tracked files)`, exit 0, no warning line. |
| 2026-09-23 | Task 9: CI workflows (`ci.yml`: verify/hygiene/db-drift, `e2e.yml`) and Dependabot | subagent | 15:30 | 15:52 | none (all four checks green on push 1) | n/a | All three files (`ci.yml`, `e2e.yml`, `dependabot.yml`) written verbatim from the brief, no deviations. `docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest -color` clean (exit 0, no output). Dependabot docker-ecosystem finding (corrected — see Task 9 fix round 1): dependabot-core's docker file fetcher matches candidate filenames against `DOCKER_REGEXP = /dockerfile|containerfile/i` (`docker/lib/dependabot/docker/file_fetcher.rb:12`, current default branch), an unanchored case-insensitive substring match, so `infra/docker/api.Dockerfile`, `migrate.Dockerfile` and `web.Dockerfile` are all discovered as-is under `directory: /infra/docker`; no gap, no rename needed. First-ever push of `feat/phase-0-bootstrap`: `.husky/pre-push` ran `turbo run typecheck test --filter='...[origin/main]'` against all 10 packages (14 tasks: 14 successful, 1 cache hit), clean. Both pre-publication gate commands (content diff and commit metadata against the terms list) printed 0 before the push. Opened draft PR #1 exactly per the controller's verbatim `gh pr create` command; both workflows triggered on `pull_request` and all four checks (`verify` 1m20s, `hygiene` 17s, `db-drift` 40s, `e2e` 1m55s — well under the 30-minute job timeout and the 15+ minute budget flagged as possible) passed on the first push, zero fix iterations. |
| 2026-09-23 | Final-review fix wave (11 items from the whole-branch review: 2 Important, 9 Minor) | subagent | 15:03 | 15:16 | none | n/a | The final review (a94fed5..cee6efc) found no Critical issues; it upgraded two deferred minors to Important (eslint-plugin-boundaries v5-era syntax under v7 printing five deprecation warnings on every lint; `gitleaks.test.sh` hardcoding `_x64`, red on arm64) and listed cheap hardening. Fixed: boundaries preset migrated to `boundaries/dependencies` + `policies` + entity selectors + `{{...}}` template, graph proven identical by a one-off 56-pair old-vs-new matrix (0 diffs), fixture test now also asserts the rendered message and zero `[boundaries]` warnings (RED 3 failed / GREEN 8/8); `gitleaks.sh` exposes its os/arch mapping via `GITLEAKS_PRINT_ASSET=1` and the test uses it (RED under an `aarch64` `uname` shim: "no checksum error message"; GREEN: `_arm64` asset, tamper assertion passes); smoke title names the project origin (6/6 e2e with distinct titles); `.superpowers/` in `.gitignore`/`.dockerignore` (redundant `.prettierignore` entry removed, Prettier reads `.gitignore`); four `.env.<environment>` deny entries; hygiene regex widened to 15 document extensions (RED 1 failed / GREEN 8/8); Postgres/Mailpit on 127.0.0.1 (LAN connect refused, Caddy still on all interfaces, `prisma migrate status` via `localhost` still works); both `main.tsx` log a failed MSW start and still render (new `main.test.tsx` per app, RED unhandled rejection + no mount / GREEN 3/3); PR-only `cancel-in-progress` and job timeouts (actionlint exit 0); formatter timeouts 2 × 25 s. `TURBO_FORCE=true pnpm verify` green (27/27 tasks, 0 cached), full stack rebuilt, `smoke.sh --full` and `pnpm e2e` 6/6. |
| 2026-09-23 | Task S1: split phase 0 into a stack of 10 small PR branches (local only, no push) | subagent | 15:27 | 15:47 | none | n/a | Why: the project owner rejected one ~16k-line PR (9.1k lockfile, 3.9k plan, ~3.75k code/docs) and chose 10 stacked PRs, one boundary each, opened at once and squash-merged in order. Branches, one commit each on `origin/main`: `phase-0/01-plan`, `02-workspace`, `03-api`, `04-web`, `05-db`, `06-compose`, `07-e2e`, `08-hygiene-hooks`, `09-claude-plugin`, `10-docs`. The shared root files (`package.json`, `pnpm-workspace.yaml`, `ci.yml`, `dependabot.yml`) were derived per branch from 3876f94 by removal only (a script whose branch-10 output is byte-identical to REF); each lockfile is REF's pruned by `pnpm install`, then `--frozen-lockfile`, with every `packages:` key present in REF (no drift, 02–10). Verified per branch 02–10: `pnpm install --frozen-lockfile` and `pnpm verify` (`TURBO_FORCE=true`); 05 `db:migrate:deploy` and `db:drift` against local postgres; 06 compose default and full profiles with `smoke.sh` / `--full`; 07 `pnpm e2e` 6/6 plus two screenshots; 08 hygiene, `@tms/scripts` tests, commitlint rejection; 09 plugin tests 61/61 and `claude plugin validate`. Final tree equals 3876f94 except `docs/client/README.md` (main's version kept, decision C) and this journal. No file moved between branches; `msw` joins `allowBuilds` in 02 (vitest's optional peer puts it in that graph) and `esbuild` (in no lockfile, REF included) arrives with Vite in 04. Lessons: pnpm checks the unpruned REF lockfile against the release-age policy before pruning, so the prune ran with REF's full `minimumReleaseAgeExclude` and the trimmed list was checked on a second install; turbo 2.11 writes a linked worktree's cache into the main checkout's `.turbo/cache` ("shared worktree cache") unless `TURBO_CACHE_DIR` points elsewhere. |
| 2026-09-23 | Chrome check (Task 12 Step 1) | inline | n/a | n/a | n/a | n/a | not run: Claude in Chrome extension not connected (nested claude --chrome -p loaded the tools; the extension reported not connected) |

## Decisions for Stefan

- Repository LICENSE: the repository is public and has none, which means all rights reserved; pick a license or confirm that is intended.
- Whether `e2e` stays a required check on `main` once its duration is known (~2 min in CI).
- Delete the now-unused local `docs/client/forbidden-terms.txt` (ADR 0007 removed the check that read it) and confirm the tracked `docs/client/README.md` (main's version, kept by the split) reads as intended.
- Add the required status checks (`verify`, `hygiene`, `db-drift`, and `e2e` per the item above) to the `main` ruleset after the stack merges.

## Inputs for later phase plans (from the final review)

- Phase 1: where the shared Nest bootstrap lives before a third copy appears (`@tms/logger` or a small bootstrap package); a `node-library` tsconfig preset vs package-local `types: ["node"]`; migrate image `deploy --prod`, `COPY --chown=node:node` and the `@prisma/dev` footprint; a boundaries test using `@tms/*` specifiers once `contracts`/`db` exist; `predev` switches to `migrate dev` + sync.
- Phase 6: regenerate the vendored MSW worker (`apps/*/public/mockServiceWorker.js`) when `msw` is bumped.
- Phase 9: cache gitleaks in CI, SHA-pin actions, Docker layer caching (`pnpm fetch`), non-root Caddy.
- Phase 10: plugin `format.mjs` case-insensitivity, temp-dir cleanup in `hooks-cli.test.mjs`.
